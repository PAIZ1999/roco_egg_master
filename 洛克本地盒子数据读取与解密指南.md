# 《洛克王国》本地精灵盒子与背包数据嗅探解密指南

本指南详细阐述了如何在本地通过**被动网络流量嗅探**和**协议实时解密**，安全、零成本地读取玩家《洛克王国》游戏内的精灵盒子及背包数据。

---

## 📖 技术原理概述

该方案采用**旁路监听（Passive Sniffing）**机制，工作原理如下：

1. **流量拷贝**：利用物理网卡驱动（Npcap）拷贝流经本机网卡的所有 TCP 数据包。
2. **静默解密**：过滤出洛克王国游戏服务器（端口 8195）的通信流量，从中提取出动态登录密钥，直接对包含精灵数据的数据包执行 `AES-128-CBC` 解密。
3. **安全隐形**：程序不向服务器发送任何数据包，也不修改游戏客户端内存，对游戏服务器而言是 100% 隐形的，**完全免除了封号风险**。

---

## 🛠️ 环境准备与依赖

1. **底层网卡驱动（必须）**：
   * 运行本工具的电脑上必须安装 **[Npcap](https://npcap.com/)**（或 WinPcap）。
   * **安装注意**：安装 Npcap 时，务必勾选 *“Install Npcap in WinPcap API-compatible Mode”*（兼容 WinPcap API 模式）。
2. **管理员运行权限**：
   * 捕获底层网卡原始包（Raw Sockets）必须具备系统高权限，因此**编译运行本代码或打包后的客户端工具，必须以管理员身份（Administrator）启动**。

---

## 📡 洛克王国 BE21 私有协议格式

《洛克王国》客户端与网关使用自定义的 `BE21` 协议帧进行通信，其结构如下：

* **包头魔法标志 (Magic)**：固定为 `b"\x33\x66"`（ASCII 字符 `'3'` 和 `'f'`，长度 2 字节）。
* **固定头部长度**：大端序（Big-Endian）排列的 **21 字节**：
  * `0 ~ 2` 字节：`\x33\x66` (Magic)
  * `6 ~ 8` 字节 (2B)：**命令字 `cmd`** (用于区分当前是什么请求/回包，如登录、打开仓库等)
  * `9 ~ 13` 字节 (4B)：包序列号 `seq`
  * `13 ~ 17` 字节 (4B)：**头部长度 `hdr_len`**
  * `17 ~ 21` 字节 (4B)：**数据体长度 `body_len`**
* **包体 (Body)**：
  * **额外头部 (Header Extra)**：位于偏移 `21` 字节 到 `hdr_len` 之间。
  * **密文 Payload (Body)**：位于偏移 `hdr_len` 到 `hdr_len + body_len` 之间。

---

## 🔑 关键解密步骤

### 步骤 1：动态截获会话密钥 (AES Key)
游戏的加密密钥并非固定，而是在每次建立连接登录时动态下发的。
* **命令条件**：当捕获到服务器返回的 **命令字为 `0x1002`** 的应答包时。
* **密钥提取**：读取该包头部中的 `header_extra` 字节段：
  ```python
  session_key = header_extra[2:18] # 偏移 2 开始的 16 字节即为本次会话的 AES-128 密钥
  ```

### 步骤 2：解密精灵数据业务包
* **命令条件**：当捕获到游戏内 **命令字为 `0x4013`**（拉取精灵盒子或背包数据）的业务包时，其数据体是被加密的。
* **加解密算法**：**AES-128-CBC**。
* **初始化向量 (IV)**：Body 密文的**前 16 字节**直接作为本次 AES 解密的 IV，即 `iv = body[:16]`。
* **密文 Payload**：从第 16 字节往后的所有数据，即 `ciphertext = body[16:]`。
* **解密与去填充**：解密出的明文字节需按照 PKCS7 标准去除尾部填充字节。

---

## 🐍 核心复刻代码 (Python 极简原型)

请先在终端安装依赖库：
```bash
pip install scapy pycryptodome
```

编写并运行以下 Python 代码（需以**管理员身份**运行）：

```python
import sys
from scapy.all import sniff, TCP, IP, Raw
from Cryptodome.Cipher import AES

# 全局状态变量
session_key = None  # 动态捕获的会话密钥
data_buffer = b""   # 拼接 TCP 分片包的缓冲区

def decrypt_pet_payload(body_data):
    """
    使用 AES-128-CBC 解密游戏业务包
    """
    global session_key
    if len(body_data) < 16:
        return
        
    iv = body_data[:16]            # 前 16 字节为 IV
    ciphertext = body_data[16:]    # 后面为密文
    
    try:
        cipher = AES.new(session_key, AES.MODE_CBC, iv)
        decrypted_data = cipher.decrypt(ciphertext)
        
        # 移除 PKCS7 填充
        padding_len = decrypted_data[-1]
        if 0 < padding_len <= 16:
            decrypted_data = decrypted_data[:-padding_len]
            
        print(f"\n[+] 成功解密业务包! 得到明文长度: {len(decrypted_data)} 字节")
        # TODO: 此时 decrypted_data 已经是解密后的精灵属性/盒子原始字节，可在此处进行反序列化或输出给主程序
        # sys.stdout.write(f"PET_DATA:{decrypted_data.hex()}\n")
        # sys.stdout.flush()
    except Exception as e:
        print(f"[-] 解密失败: {e}")

def parse_be21_frame(frame_data):
    """
    解析洛克王国自定义 BE21 协议帧
    """
    global session_key
    if len(frame_data) < 21:
        return
        
    cmd = int.from_bytes(frame_data[6:8], byteorder="big")
    hdr_len = int.from_bytes(frame_data[13:17], byteorder="big")
    body_len = int.from_bytes(frame_data[17:21], byteorder="big")
    
    # 情况 A：捕获 0x1002 包提取 AES 会话密钥
    if cmd == 0x1002:
        header_extra = frame_data[21:hdr_len]
        if len(header_extra) >= 18:
            session_key = header_extra[2:18]
            print(f"\n[+] 捕获到动态 AES 会话密钥: {session_key.hex()}")
            
    # 情况 B：捕获 0x4013 包进行精灵盒子解密
    elif cmd == 0x4013:
        if session_key is None:
            print("\n[-] 发现精灵盒子数据包，但由于未捕获密钥，无法解密。(请在运行此工具时重新登录一次游戏)")
            return
        body_data = frame_data[hdr_len : hdr_len + body_len]
        decrypt_pet_payload(body_data)

def tcp_packet_callback(packet):
    """
    网卡流量捕获回调
    """
    global data_buffer
    if packet.haslayer(TCP) and packet.haslayer(Raw):
        payload = packet[Raw].load
        data_buffer += payload
        
        # 循环解析缓冲区内的 BE21 数据帧
        while len(data_buffer) >= 21:
            # 校验帧头魔数
            if data_buffer[0:2] == b"\x33\x66":
                hdr_len = int.from_bytes(data_buffer[13:17], byteorder="big")
                body_len = int.from_bytes(data_buffer[17:21], byteorder="big")
                total_len = hdr_len + body_len
                
                # 若当前缓冲区数据已接收完毕一个完整的协议帧，进行解析
                if len(data_buffer) >= total_len:
                    current_frame = data_buffer[:total_len]
                    parse_be21_frame(current_frame)
                    data_buffer = data_buffer[total_len:] # 移出解析过的包
                else:
                    break # 数据帧未完整接收，等待后续网络包
            else:
                # 丢弃非洛克协议的脏字节
                data_buffer = data_buffer[1:]

if __name__ == "__main__":
    print("[*] 正在启动洛克王国流量监听解密器 (端口 8195)...")
    print("[*] 注意：请务必确保已安装 Npcap 且正以管理员权限运行...")
    # 开启本地 8195 端口的 TCP 流量被动监听
    sniff(filter="tcp port 8195", prn=tcp_packet_callback, store=0)
```

---

## ⚠️ 避坑与进阶建议

### 1. 应对大体量数据：TCP 字节流分片重组 (TCP Segmentation)
当玩家的精灵仓库（盒子）里有上千只精灵时，服务器返回的数据体积会非常巨大（几百 KB 甚至数 MB）。这在底层网络中会被拆分为无数个小 TCP 分片（Segment）进行传输。
* **简易缓冲区的缺陷**：上述原型代码中的 `data_buffer += payload` 属于极简实现，在网络波动、丢包重传或包乱序时可能会发生错乱，导致长字节大包解密失败。
* **工业级解决方案**：您在实际项目中应当依据捕获的 TCP 包中的 `seq`（序列号）与 `ack`（确认号），在内存中为双向的数据流建立可靠的排序重组逻辑（参考 `rocom-helper` 的 `reassembly.py` 文件实现），拼装成完全连续的字节流之后，再使用 `0x33 0x66` 提取出完整的 BE21 协议帧。

### 2. 桌面客户端的开发与分发架构
如果您使用 **Electron / Tauri** 等前端框架开发您的电脑端工具：
* **不推荐在 Node.js 中编译原生网卡库**（如 `node-pcap`），因为这在 Windows 平台的 node-gyp 编译中极其容易报错，且环境配置极为复杂。
* **推荐架构**：
  1. 将上述 Python 原型代码通过 `pyinstaller --noconsole -F core_sniffer.py` 打包成一个体积小巧的后台守护程序 `sniffer.exe`。
  2. 在 Electron/Tauri 主进程中，通过 `child_process.spawn` 启动这个 `sniffer.exe`（记得在 Windows 的清单文件中配置 `requireAdministrator` 以自动向用户请求管理员权限）。
  3. 通过监听 `stdout` 管道读取解密出的明文十六进制流或 JSON，并在渲染进程中渲染出精美的 UI 界面。
