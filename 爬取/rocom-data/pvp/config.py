"""PVP 自动战斗配置 — 咔咔鸟脚本"""

# ── 模板参考高度 ──────────────────────────────────────────────────────
# 截图时游戏窗口的高度（像素）。运行时会按 当前窗口高/此值 缩放模板，
# 使同一套截图在更大的窗口下也能匹配。
# 把你截图时的窗口高度填在这里（用画图打开截图看图片高度即可）
TEMPLATE_REFERENCE_HEIGHT = 736   # 根据模板图片尺寸推算；如截图时窗口高度不同请修改此值

# ── 扫描区域：左半边游戏窗口（启动时自动覆盖，无需手填） ────────────
LEFT_HALF = (0, 0, 9999, 9999)   # 占位，battle.py 启动时自动设置

# ── 开局序列（各执行一次）────────────────────────────────────────────
OPEN_SEQUENCE = [
    "skills/暴风眼.png",
    "skills/追打.png",
    "skills/音波弹.png",
    "skills/聚盐.png",
]

# ── 主循环技能优先级（依次尝试，找到哪个用哪个）──────────────────────
MAIN_SKILLS = [
    "skills/音波弹.png",
    "skills/聚盐.png",
]

# ── 开始挑战按钮模板 ──────────────────────────────────────────────────
# 从 kingdom 项目复制；如果 PVP 界面按钮样式不同需重新截图替换
START_CHALLENGE_TEMPLATE  = "start_challenge.png"
START_CHALLENGE_SIMILARITY = 0.7

# ── 换宠面板检测模板 ──────────────────────────────────────────────────
# 咔咔鸟死亡后左下角出现 "♥ 数字" 心形图标，截心形部分（不含数字）作为模板
# TODO: 从换宠面板截图中裁出左下角心形图标，保存到此路径
SWITCH_PANEL_TEMPLATE = "pvp/switch_panel_heart.png"

# 检测到面板后按哪个键选精灵（按下即换宠，无需确认）
# 截图中槽1和槽2有三角形警告图标，按 3 更安全
SWITCH_ELF_KEY = "3"

# ── 相似度 ────────────────────────────────────────────────────────────
SKILL_SIMILARITY  = 0.75
PANEL_SIMILARITY  = 0.70

# ── 时间参数（秒） ────────────────────────────────────────────────────
DARK_TIMEOUT     = 2.0
POST_SKILL_DELAY = 1.5
POLL_INTERVAL    = 0.3
SWITCH_DELAY     = 1.0
