# 数据存储配置 — 本机开发请复制为 .env 并填入真实值
# 设置为 true 启用数据库存储，false 则使用JSON文件（默认）
DB_ENABLED=false

# MariaDB连接信息（仅 DB_ENABLED=true 时使用）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=rocom_data
