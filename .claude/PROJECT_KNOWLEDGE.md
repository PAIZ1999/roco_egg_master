# 项目知识库: 洛克王国孵蛋数据管理系统 (桌面打包与本地数据存盘)

## 📌 项目概述
该项目是洛克王国宠物孵蛋数据管理系统，基于 Vite + React + TailwindCSS 开发。经过 Electron 移植，现已支持打包为双击运行的 Windows 桌面 `.exe`（便携免安装单文件版），并实现将数据自动持久化到本地物理文件夹中的 JSON 文件中。

---

## 🛠 架构设计与技术栈

1. **渲染进程 (Vite + React)**:
   - 包含主界面展示、筛选条、自动保存状态栏。
   - 文件通过 `vite.config.ts` 中的 `base: './'` 使得构建 of 静态资源支持以 `file://` 协议正确读取。
   - **配对中心与长图导出及表格颜色优化 (2026-07-09 新增)**:
     - **同一个母本仅导入当前显示的父本配对**：将繁育配对选择池从包含所有隐藏配对的 `filteredPairings` 调整为各 eggSprite 分组当前由 Chevron 所选的 `activePairings`，默认同一个母本卡片只选中/导入当前选择显示的父本，防止非显示下的隐藏配对被误导入。
     - **配对一键全选与取消全选**：在配对中心 Header 头部新增“全选”与“取消全选”控制按钮，支持一键清空选中然后自主勾选。
     - **长图导出深度过滤与表格列重排**：
       - 除原有的操作按钮隐藏外，通过直接操纵克隆 DOM 树彻底移除主 Banner 顶栏、Tab 导航栏、所有统计面板及筛选搜索行，只导出表格或卡片核心内容区域。
       - **长图导出蛋窝表格列重排**：在长图导出时，自动将蛋窝表格的列顺序调整为图二形式：`#`、`精灵`、`牌子`、`性格`、`现蛋`、`蛋窝状态`、`蛋组`、`三维`，确保分享图视觉感极佳且隐藏冗余操作列。
     - **浅色主题表格视觉优化**：将蛋窝表格模式、父本表格模式、母本表格模式在浅色主题下的隔行背景斑马纹重新规范为：奇行 `#F2F6F8`，偶行 `#ffffff`，悬停 hover 颜色为 `#DFE9EF`，提供高对比度的现代轻量阅读感。
     - **3V 与同性格交叉筛选及解耦**：将配对中心和蛋窝中心的 3V 筛选和性格匹配关系拆分为两个独立的下拉筛选组件，允许用户交叉筛选（如“同性格 3V”），并新增“仅非同性格 3V”高级筛选项。
     - **表格列编辑顺序与列宽调整**：
       - 将蛋窝表格在编辑状态下的列顺序调整为图一形式：`#`、`精灵`、`蛋窝状态`、`现蛋`、`性格`、`牌子`、`蛋组`、`三维`、`操作`。
       - **优化列宽以消除大片间距空白**：将精灵列从原先的 19% 缩窄至 12% 以消除第一个红框右侧的大片空白；将三维列从原先的 19% 大幅拉宽至 31%，保证三维（尤其是父母不同的双层三维）能有充足舒展空间，从而完美吃掉并消除了蛋组列与三维列、三维列与操作列之间的空洞间距，实现整表视觉上的绝对平衡与平整。
       - 将蛋窝表格分页数 NEST_PAGE_SIZE 从 50 修改为 25。
       - **现蛋双微调控件去除**：通过加入 Tailwind 针对 HTML5 number 属性的特定伪类清除类 `[appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none` 彻底隐去浏览器原生自带的灰色调节箭头，只保留我们自己定制的橘色微型箭头，保证在各个浏览器里的展示极度纯净且防重。
       - **现蛋数量与蛋管理中心双向联动**：当在表格中直接修改现蛋数量或通过箭头加减时，系统会自动与“精灵蛋管理中心”联动。数量增加时会自动在其顶部追加对应的精灵蛋卡片（自动获取最低进化阶段并标记 `fromNestId`），数量减少时会依次寻找该窝点生成的蛋并按最新程度删减，从而保持物理数量绝对同步。现蛋数量改为 0 时自动改状态为“正在孵，可预约”。
       - **有现蛋初始值归 0**：在卡片模式或表格模式下，当用户将蛋窝的“蛋窝状态”修改切换为“有现蛋”时，默认现蛋数量直接设为 0。
     - **父母本统计表格（WarehouseStatsTable）高级增强**：
       - **精灵蛋组智能查询**：在统计表格顶部加入 Autocomplete 智能联想精灵输入框，支持输入名字快速查询，在顶部详情区展现对应的精灵头像、系别、蛋组，同时在下方大表格中以琥珀金色分割聚光灯高亮该精灵对应的蛋组列。
       - **只看种公统计**：支持一键开启“只看种公”模式，在此模式下，系统会在统计分析、已收集达成格子判断中排除全部种母，只亮起种公的收集状态。
   - **网页端部署静态资源拷贝**：为解决网页端（如 Vercel、Cloudflare Pages）部署后根目录 `images` 文件夹内图片丢失导致图片裂开的问题，新增了 `copy-images.js` 脚本，并在 `package.json` 中的 `build` 命令后追加 `node copy-images.js`，使打包后自动将根目录 `images` 递归拷贝至 `dist/images`。
    - **桌面宠物悬浮球与主窗口内拖拽及多向拉伸与阻断滚动穿透优化 (2026-07-09 新增)**:
      - **独立悬浮球组件挂载**：在 `App.tsx` 区分独立窗口（`isFloatWindow`）时，联合 URL Hash (`#float`) 与 Electron 进程参数 `--window-type=float`（通过 `window.electronAPI.isFloatWindow()` 检测）双重判定，确保渲染 `MerchantFloatWidget` 时正确传入 `isStandalone={true}`，采用 `w-full h-full p-2` 紧凑尺寸填充 70x75 视口，防范单靠 Hash 传递在打包加载中丢失导致溢出裁剪无法显示的问题。
      - **游戏数据提取形态智能还原 (2026-07-10 新增)**：当从游戏 SQLite 直连或 API 提取宠物时，由于默认名字仅含基础名称（如“鸭吉吉”、“丢丢”），在此建立基于 `conf_id` 与属性还原的形态解析映射：
        - **时序优化**：将原本在后缀拼接之后的兜底名字解码逻辑移到前置，使形态判定和拼接时 `rawName` 具备正确的精灵基础名称，彻底解决“起来鸭”等形态因 `rawName` 暂空而错过形态后缀拼接、最后退化为“蓬松的样子”的 Bug。
        - **鸭吉吉（3742/起来鸭等）形态映射**：
          - `3742001` -> 蓬松的样子（鸭吉吉的默认形态）。
          - `410738` -> 起来鸭（戴睡帽的鸭吉吉）。
          - `300710` -> 紧实的样子。
        - **丢丢进化链（丢丢、卡卡虫、卡瓦重）形态映射**：
          - `2200004` -> 草地附近的样子。
          - `Math.floor(conf_id / 10000) === 329` (如 `3290001`/`3291001`/`3292001`) -> 沙地附近的样子。
          - `300036` -> 雪山附近的样子。
          - `410036` -> 火山附近的样子。
        这彻底解决了导入时多形态混淆丢失、野外亚种还原为错误默认图的问题。
      - **主界面喵喵随意拖拽**：在 `MerchantFloatWidget.tsx` 中增加 React 组件级 position 状态定位，计算拖动 delta。若在主窗口（`isStandalone === false`）内则在 DOM 级更新 right 与 bottom 定位，并设置边界值防超出窗口，而非一律调用 Electron 移动独立窗口的 IPC `dragFloatWindow`；在独立窗口中（`isStandalone === true`）则继续执行 `dragFloatWindow` 移动整个桌面悬浮窗。
      - **多向拉伸支持**：在 `MerchantFloatWidget.tsx` 展开聊天室时，除原有的左边缘和顶边缘拉伸外，在左上角追加了 `cursor-nw-resize` 鼠标手势的对角线拉伸热区，拖拽时同时调节聊天窗口的宽度和高度，并伴有斜线折角视觉引导线。
      - **滚动穿透深度隔离**：为阻断消息历史区域、父本仓储列表和母本仓储列表由于过度滚动（overscroll）导致整个软件外层页面上下滚动的现象，为这三处滚动容器均物理应用了现代 CSS `overscroll-behavior: contain` 规则，实现零 JS 开销的安全滚动隔离。
      - **智能助手随主题切换亮暗色模式**：对 `MerchantFloatWidget.tsx` 中的折叠图标、聊天室外壳、头部、消息历史背景、输入发送区以及所有自定义卡片气泡（`ChatPetCard`，`ChatEggGroupCard`，`ChatEggPredictCard`，`ChatMerchantCard`）均做了深浅双色主题（Tailwind `dark:`）重构，保证了高对比度及自适应主题的极简设计。
      - **系统偏好设置与重置**：在 About（关于）模态框的 Body 中集成了“系统工具”面板，新增“重置关闭行为确认提示”按钮，方便用户一键清除主进程已记录的关闭行为。
    - **智能 3V 蛋与极限蛋统计检测**:
     - **3V 蛋**: 当父母三围（生命、物攻、速度等）设置完全一致时，系统会自动将该宠物行高亮，并在表头左侧显示“3V蛋”标志。只有当父母三围一样且窝点状态为“有现蛋”时，才会统计入顶部的“3V蛋”总数。当任一三围被设置为“无”或者三围被隐藏时，系统会自动将该行排除在 3V 统计之外。
     - **极限蛋**: 只有当选择为“极限”且窝点状态为“有现蛋”时，才会统计入顶部的“极限蛋”总数.
    - **体型声音临界牌子与联合强力过滤 (2026-07-08 新增)**:
      - 智能推算临近大块头（`isNearGiant`：身高已达标，体重在大及格线的 90% 到 100% 之间）与临近声音（`isNearVoice`：叫声偏差绝对值在 `[90, 95]` 之间）。
      - 导入页面新增四个强力过滤项：`临近大块头`、`临近声音`、`大块头 + 临近声音`、`声音牌 + 临近大块头`，帮助玩家极速定位极佳的临界备选父母本。
    - **大容量数据导入与全面分页渲染优化 (2026-07-08 新增)**:
      - **O(1) 哈希去重算法**：在 `RocoImportModal.tsx` 中，放弃以往的线性 `.some(...)` + 对每个精灵 `JSON.stringify` 带来的巨大 CPU 负荷。改为在大循环前，提前把 parents 编译为唯一的哈希特征 `Set`，将去重性能复杂度从 $O(N \times M)$ 优化到 $O(N + M)$，彻底消除几千只宠物导入时的解析挂起假死（白屏）。
      - **预览与仓储三端分页（Pagination）机制**：
        - 导入预览弹窗限制单页展示 50 行，底部提供精美分页控制器，避免一次性渲染数千个表格 DOM 节点。
        - 父母本仓储父本与母本各自独立分页展示，限制单页 10 张卡片，在保证黄金繁育配对的同时大幅减轻 DOM 重绘开销。
        - 蛋窝中心网格改版为每页 9 个蛋窝（3x3），底部提供与精灵蛋分页统一的带有省略号的高颜值分页控件。支持一键在“卡片网格模式”与“数据表格模式”之间自由切换，模式选择持久化存储在 localStorage 中。
      - **过滤联动与页码归拢**：当在各页面中重置或更改过滤项、改变搜索文本时，自动重置页码为 1；并在发生删除或数据减少超出总页数时自适应归拢当前页码，确保数据高一致性。
    - **蛋窝数据表格模式与自适应分行展示 (2026-07-08 新增 & 视觉二次重构优化)**:
      - 提供了极其精致的表格视图切换（支持 `LayoutGrid` 卡片模式与 `Table` 表格模式双向切换）。
      - 表格模式中，数据自适应合并与深度扁平化美化：
        - **性格与三维**：性格全称被清洗并精简为简称展示。若父母方属性完全相同时合并为单行，不同时采用淡雅中性的灰色 `♂` 与 `♀` 指标分两行展示，文字使用高对比度中性色，根绝了大面积明亮色块的堆叠。
        - **牌子与蛋窝状态 select 胶囊 Badge 化**：通过应用 `appearance-none border-0 rounded-full py-1 shadow-3xs` 极细微阴影，使表格内的 select 控件在视觉上完美变身为圆角胶囊 Badge 标签，兼顾点击下拉修改的快捷性与高观赏性。
        - **现蛋数量列智能降噪**：非“有现蛋”状态时隐藏输入框和微调箭头，居中展示精简置灰的 `0` 占位 Badge；“有现蛋”时才启用淡黄色高亮 Badge 包装的数字输入微调框，有效降噪。
        - **去纵向网格线与等宽列距**：彻底剔除表格内的全部纵向分割线 (`border-r`)，并应用 `table-fixed` 及统一划分的黄金比例列宽，实现极为均称、对齐平滑的现代极简扁平数据报表排版。
        - **隔行交替斑马纹**：偶数行呈现极淡的青灰蓝色背景 (`bg-[#f0f7ff]/60`)，奇数行纯白，带来高层次的结构感，移除了 3V 行的绿色背景干扰，使长图导出和桌面阅读体验高度一致。
        - **文字及头像放大增强清晰度**：将精灵头像大小扩展为 `w-10 h-10`，精灵名放大至 `text-[13px] font-black`，其他文字也调大为 `text-[12.5px]` 或 `text-xs font-semibold`，确保桌面缩放和窄屏下的绝对清晰可读性。
      - **导出长图同步**：当用户切换为表格模式时，导出的长图也同步呈现为精美的数据报表。通过为“操作”、“切换控制器”注入 `action-buttons` 标记，使导出图片时完美隐去垃圾箱、调整箭头与分页控制组件，产出极致整洁的高品质长图。
        - **多形态精灵支持与交互极细化**:
           - 通过在精灵名称中使用下划线后缀表示特定形态（如 `冬羽雀_春天的样子`），底层重构的 `petHelper` 能够剥离出基础精灵名，从而让多形态精灵也能够完美匹配对应的系别和蛋组。
           - **游戏数据提取形态智能还原 (2026-07-10 新增)**：当从游戏 SQLite 直连或 API 提取宠物时，由于默认名字仅含“鸭吉吉”，在此针对鸭吉吉（3742）建立基于 `conf_id` 的形态解析映射：
             - `3742001` -> 蓬松的样子（鸭吉吉的默认形态）。
             - `410738` -> 紧实的样子（戴睡帽的鸭吉吉）。
             - `300710` -> 起来鸭。
             这彻底解决了导入时多形态混淆丢失的问题。
           - **切换形态数据联动修复 (2026-07-10 新增)**：修正了以往 `getPetDetails` 等函数对形态全名的暴力截断 Bug。改为优先匹配完整形态全名（或括弧/下划线转换名），找不到再降级使用 `baseName`。这使得在父母本卡片中切换不同的“样子”时，系别、三围及达标线区间指南等能做到实时数据联动更新。
           - **图片前缀模糊兜底优化 (2026-07-10 新增)**：优化了 `getSpriteFileName`。由于部分多形态精灵无 exact 默认图，在模糊匹配前缀时优先寻找本来的样子/平常的样子，若为“鸭吉吉”则额外优先兜底至“蓬松的样子.png”，解决了在没有形态名时被随机归到匹配名首位的“急急急鸭.png”问题。
           - **分支进化与同阶升级重构**：针对在同一进化链上存在平行的多个最终形态（例如“乖乖鹄 → 蓝珠天鹅 → 翠顶夫人 → 黑羽夫人”中的翠顶夫人与黑羽夫人均为 Stage 3；“墨鱿士 → 混乱鱿彩 → 秩序鱿墨”中的混乱鱿彩与秩序鱿墨均为 Stage 2），在 `src/petHelper.ts` 中引入了 `chainMaxStageMap` 预处理。若当前宠物本身已是同链最大形态之一（如“翠顶夫人”或“混乱鱿彩”），则禁止同阶升级以保留其本身；若是低阶形态（如“乖乖鹄”），则默认指向该进化链的最后一个最终态（如“黑羽夫人”）。
           - **形态下拉框选项文字定制**：在所有相关页面（蛋窝卡片、表格行、换蛋发布看板、换蛋需求看板）的头像形态切换下拉框中，将原本显示为“默认”的最终形态选项进行文本精细化转换：若是翠顶夫人，则显示为“翠顶”；若是黑羽夫人，则显示为“黑羽”；若是秩序鱿墨，则显示为“秩序”；若是混乱鱿彩，则显示为“混乱”——使其完美与其最终进化的名字对应。
           - 蛋窝卡片、表格行、换蛋发布表单以及换蛋需求卡片的头像上均集成了毛玻璃微调形态下拉菜单，点击即可实时切换并更新预览。
           - 自动升级进化链时（如在换蛋卡片或主表中选择进化前精灵）会自动保留形态后缀。
           - 所有的形态选择下拉菜单都加上了 `.action-buttons` 类，使长图导出时能够自动隐形，极简清爽。
           - 换蛋卡片的 hover 删除按钮改为了卡片右上角的淡入垃圾桶按钮，解决了原本 hover 覆盖头像时遮挡多形态下拉菜单的交互痛点。
            - **多形态图鉴与助理错乱修复 (2026-07-11 新增)**：
              - 对 `pets_data.json` 中矿晶虫 (ID 39)、毛毛 (ID 32)、脆筒甜甜 (ID 234) 等分支进化/多形态精灵进行物理数据去重，彻底消除了 forms 与 evolution_chain 数组中由于历史生成 Bug 重复累加的脏数据。
              - 将数据查询中心（`DataQueryTab.tsx`）与桌面助理（`MerchantFloatWidget.tsx`）中的多形态切换，由脆弱的**数组索引 `idx` 关联**重构为**基于形态后缀名称（`styleName`）检索匹配**，完美解决了 avatars 与 forms 数组在顺序和长度上不一致导致的雷达图六围资质与达标线错乱的 Bug。

    - **洛克王国专属 30 性格清洗系统**:
     - **30 种有效加成性格**: 严格遵循《洛克王国：世界》设定，项目去除了所有“平衡”性格（如实干、认真、浮躁），仅保留 30 种有属性加成的有效性格和 1 个“错性格”。
     - **全生命周期智能数据清洗与空性格值处理**: 提供了 `cleanNature` 清洗模块。当从本地磁盘、localStorage 或备份中加载老旧数据时，会自动将旧的简称（如 `逞强`）或仅含加减号的属性（如 `+物攻 -生命`）无缝补全为全称（如 `逞强 (+物攻 -生命)`）。若遇到已废弃的旧版平衡性格或其它无效字符，则自动转换并兜底为 `"错性格"`，确保了数据的绝对纯净和零宝可梦残留。**特别地，在 2026-06-06 的优化中，添加精灵时的父/母性格默认初始化为空字符串 `""` 并显示为“选择性格”占位符，直接点击即可自动弹出推荐性格下拉列表；`cleanNature` 会特殊保留空字符串和“选择性格”，使其不再被误重置为“错性格”。**
   - **三围隐藏与“无”选项**:
     - **“无”选项**: 父母三围列表选项中新增了“无”，用于标记用户不想填写某项三围的情况，方便区分未设定和已设定的状态。
     - **隐藏三围开关**: 每一行表格的三围数据列都带有一个“隐藏三围”按钮，点击后三围选择框隐藏，只在表格内留白并显示“三围已隐藏”字样以及一个“显示并填写”按钮。
   - **列物理布局与长图导出**:
     - 物理排列顺序为 `牌子 -> 窝点详情 -> 是否极限`，且去掉了包裹 table 容器的 `no-scrollbar` 样式。这样在窄屏时可以通过原生滚动条平滑横向滚动，避免元素被圆角卡片裁剪，确保全部列完全可访问。
     - **长图导出深度优化**: 导出长图时会在 `document.body` 动态添加 `exporting` 样式类，结合 `index.css` 全局规则，不仅过滤垃圾桶、排序手柄、筛选框、下拉框小箭头，还可以将隐藏三围状态下的“显示并填写”等一系列页面动作按钮（带有 `.action-buttons` 类）完美隐藏，使用户得到的长图绝对清爽和完美。
      - **牌子选项扩充与“单小不点”新增**: 品牌选项（`brand`）中除原先的“大粗、大婉、单大块头”外，引入了“小粗”、“小婉”以及“单小不点”牌子，并将“单大粗”和“单大婉”分别重命名为“单粗嗓门”和“单婉转声”，赋予它们优雅醒目的高饱和度/轻量化渐变底色。
      - **体型牌极限渲染规则**: 
        - 只有在到达极限值时才显示“极限大”或“极限小”。
         - 选择体型牌（如大粗、大婉、小粗、小婉、单大块头、单小不点）时，在未达极限时不显示“大块头 (达标)”以及“临界”等字样，无徽章。
         - 非体型牌（如普通、单粗嗓门、单婉转声）在差值 10% 以内显示临界信息，且达标时显示“大块头 (达标)”/“小不点 (达标)”。
         - **蛋卡片体型标签过滤规则 (2026-06-13)**: 精灵蛋的牌子为 `"大粗"`, `"大婉"`, `"单大块头"` 时，如果在未达极限大值时，不展示默认的 `"普通体型"` 标签，避免误导。同时移除了卡片左下角的“孵化时间”小标签，并自适应在“标准区间”底部新增了“大及格”/“小及格”的临界值身高体重指示面板，与父母本卡片信息一致。
      - **下拉框 Portal 挂载优化**: 针对精灵名称和性格下拉框在表格行和横向滚动容器中容易被裁剪遮挡的问题，将 Autocomplete 组件的下拉菜单通过 React Portal 挂载到 `document.body` 上，并动态实时计算输入框在视口中的位置，实现了滚动和缩放下的自适应绝对定位。
     - **长图导出排版与对齐优化**:
       - 调大了“精灵名称”列的宽度占比（主表格 14%，导出克隆表 15%），避免长图导出时因为列宽不足导致宠物名字折行。
       - 在将 `input`/`select` 转换成静态 `div` 时，自适应提取其 `text-left`/`text-right` 等对齐属性，并映射到 `justifyContent` 与 `textAlign`，确保导出的长图文字完全对齐不跑偏。
       - **DPI与Zoom缩放自适应校正**: 在启用了 Windows 系统缩放（如 125%、150%）或浏览器页面缩放的电脑上，通过 `getComputedStyle` 取得的物理像素值（如 `fontSize`, `lineHeight` 等）会被乘以 DPR 放大。在导出克隆为静态 `div` 时，停止调用物理像素除以 DPR 重写字号的逻辑，而是完美让其继承原有的 Tailwind 文本字号类（如 `text-[10px]`, `text-xs` 等）和自适应排版。同时，对高度、宽度、padding 等外廓和定位像素进行 `adjustPx` 转换。这彻底规避了 Chromium 内核对中文字体“最小 12px 保护限制”引起的字体溢出容器和换行 Bug，使长图在任意电脑缩放比例（1.25x, 1.5x, 2.0x）下均能稳定、高清地输出，完美无折行。
     - **系别 Badge 纯图标化展示**: 删除了属性 Badge 里的系别中文文字，仅以圆形图标（外加 hover title 说明）呈现，确保界面在小屏幕与导出长图中更加紧凑美观。
      - **自建换蛋需求中心卡片蛋组集成与防抖动排版**:
        - 在换蛋卡片（看板）中引入精灵蛋组 Badge 的显示，支持多蛋组渲染，并复用主表格的 `getEggGroupStyle` 函数确保视觉一致性。
        - 针对“错性格”等长性格名情况，通过将性格/牌子容器行与蛋组容器行完全分离为独立的块级垂直布局，解决了文本溢出挤压或遮挡蛋组的问题，保证了卡片在极端文字长度下的高度自适应与导出长图的美观性。
     - **精灵头像悬浮删除按钮**: 移除了原有的普通删除按钮，重构为完全嵌在精灵头像容器（`relative group/avatar`）内的绝对定位遮罩按钮。在 hover 时通过不透明度渐变（`group-hover/avatar:opacity-100`）呈现半透明红色蒙版及垃圾桶图标，并让头像图片微缩（`group-hover/avatar:scale-75`），提供高品质微交互效果。同时该按钮带有 `.action-buttons` 类，可在导出长图时自动过滤。此外，精灵蛋卡片删除动作去除了确认弹窗，实现一键秒删。
     - **精灵蛋窝卡片化与易用性精细打磨**:
       - 将原来杂乱的列表重构为卡片化网格，使精灵名显示在头像正下方。
       - 删除了父母性格旁的 `+` 增加性格按钮以保持布局简约；将三围图标的底盘变更为白底带浅色边框与微阴影，以高对比度衬托彩色 PNG 属性图片。
       - **蛋窝卡片排版重构与美化 (2026-06-09)**:
         - 合并状态/牌子/极限：将原来的“牌子”、“极限蛋”与“蛋窝状态”选项合并到同一行，使用 12 列自适应网格（`grid-cols-12 gap-1`）紧凑排版，腾出垂直空间并提高视觉整体感。
         - 蛋组视觉隔离：蛋组配置区域加上了优雅的浅色背景底框（`bg-slate-50/70 p-1.5 rounded-lg border border-slate-100/60`）并带有高对比度的 Teal 彩色 Badge，起到了极佳的视觉分区作用。
         - 调大核心字体：所有核心字段输入和下拉选择均被调大或加粗（下拉选择框调大至 `text-xs font-bold`，性格输入框调大至 `text-[13px] font-semibold`），在屏幕端和 1200px 高清长图导出中均保持极高的清晰度与可读性。
       - **状态选项简化与新旧数据迁移 (2026-06-09)**:
         - 将原本冗长的 `"已撤窝，要提前换产线"` 状态缩减为简明扼要的 `"已撤窝"`，并统一在类型、下拉、样式映射中对齐。
         - 在 `src/App.tsx` 数据加载模块的 `migratePets` 方法中加入历史数据过渡检查，若加载的历史数据中存在 `"已撤窝，要提前换产线"` 时，将自动原地无缝迁移为 `"已撤窝"`，保证用户已有存储的绝对向下兼容性。
       - 主卡片区上方新增“我的精灵蛋窝中心”标题栏，并在长图导出时动态将标题文本替换为“我的窝点”，使长图的上下排版风格达到完全的对称与美观。
       - **小标题一致性与卡片排版细节极致打磨**：
         - 完美拉齐了“我的精灵蛋窝中心”与“自建换蛋需求中心”标题栏的内边距、标题大小（`text-lg font-bold`）、说明小字（`text-xs mt-0.5`）、图标尺寸（`w-5 h-5`）与 Badge 大小。
         - 左侧列将头像、名字输入框、系别图标封装在 `gap-1.5` 的 Flex 容器中，且将名字输入框的底边距替换为对称 `py-0.5`，实现了头像-名字-系别的像素级等距垂直紧凑排版。
         - 右侧列布局从 `justify-between` 变更为 `justify-start gap-1.5`，消除未选择“有现蛋”时元素被强制拉开产生的中间空虚感。
         - 当窝点状态为非“有现蛋”（如“正在孵”、“已撤窝”、“投资中”等）时，在底部自动渲染一行在高度、外边距、内边距上与“现蛋数量输入”完全对齐的占位说明色带（包含自适应背景、边框及呼吸灯指示点），从而在所有状态下保持卡片整体高度恒定且高度饱满，彻底优化了长图导出与屏幕渲染。
        - **换蛋卡片精灵头像放大与自适应**:
          - 将自建换蛋卡片的头像容器由原本的 `w-16 h-16` 扩大为 `w-20 h-20` (80px)，并将内部精灵图片的尺寸调整为 `w-16 h-16`，Egg 占位图标设为 `w-10 h-10`，右下角属性图标调整为 `w-6.5 h-6.5`（内部图片 `w-4.5 h-4.5`），使得精灵主体在视觉上更加瞩目且清晰，整体布局在拉大后完美自适应，长图导出无溢出。
        - **多形态精灵支持与交互极细化**:
          - 通过在精灵名称中使用下划线后缀表示特定形态（如 `冬羽雀_春天的样子`），底层重构的 `petHelper` 能够剥离出基础精灵名，从而让多形态精灵也能够完美匹配对应的系别和蛋组。
          - **分支进化与同阶升级重构**：针对在同一进化链上存在平行的多个最终形态（例如“乖乖鹄 → 蓝珠天鹅 → 翠顶夫人 → 黑羽夫人”中的翠顶夫人与黑羽夫人均为 Stage 3；“墨鱿士 → 混乱鱿彩 → 秩序鱿墨”中的混乱鱿彩与秩序鱿墨均为 Stage 2），在 `src/petHelper.ts` 中引入了 `chainMaxStageMap` 预处理。若当前宠物本身已是同链最大形态之一（如“翠顶夫人”或“混乱鱿彩”），则禁止同阶升级以保留其本身；若是低阶形态（如“乖乖鹄”），则默认指向该进化链的最后一个最终态（如“黑羽夫人”）。
          - **最高阶平行形态跨宠物切换**：在 `getAvailableSprites` 中，若宠物处于最终阶段且该链有多个平行最高阶，会同时将所有平行的最高阶形态（及各自的下划线子形态）作为可用形态合并返回。使用户在点击头像形态选择下拉框时，能直接跨宠物在“翠顶夫人”与“黑羽夫人”、或者“秩序鱿墨”与“混乱鱿彩”之间任意切换，切换后对应卡片/行系别与属性动态刷新。
          - **形态下拉框选项文字定制**：在所有相关页面（蛋窝卡片、表格行、换蛋发布看板、换蛋需求看板）的头像形态切换下拉框中，将原本显示为“默认”的最终形态选项进行文本精细化转换：若是翠顶夫人，则显示为“翠顶”；若是黑羽夫人，则显示为“黑羽”；若是秩序鱿墨，则显示为“秩序”；若是混乱鱿彩，则显示为“混乱”——使其完美与其最终进化的名字对应。
          - 蛋窝卡片、表格行、换蛋发布表单以及换蛋需求卡片的头像上均集成了毛玻璃微调形态下拉菜单，点击即可实时切换并更新预览。
          - 自动升级进化链时（如在换蛋卡片或主表中选择进化前精灵）会自动保留形态后缀。
          - 所有的形态选择下拉菜单都加上了 `.action-buttons` 类，使长图导出时能够自动隐形，极简清爽。
          - 换蛋卡片的 hover 删除按钮改为了卡片右上角的淡入垃圾桶按钮，解决了原本 hover 覆盖头像时遮挡多形态下拉菜单的交互痛点。

    - **父母本管理中心与智能繁育配对**:
      - **父母本卡片管理**: 新增“父母本管理”Tab，支持分别录入父本与母本精灵卡片。每张父母本卡片限制仅可选择一个性格（`nature: string`）和一组三围属性（非“无”），并且身高和体重输入框分别集成 Lucide Ruler 图标（单位 `m`）与 Lucide Weight 图标（单位 `kg`）。名字和性格选择支持像蛋窝卡片一样的 Autocomplete 模糊搜索，并根据选择 the 精灵自动从数据包中匹配对应的蛋组。
      - **父母本滚动查看布局 (2026-06-10)**: 彻底删除了原先的 `parentCols` 列数滑块，使左右分栏列表恢复为原生纵向滚动排布（Grid 容器恢复为 `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-1 xl:grid-cols-2 gap-4`），提供纯粹的桌面与大屏滚轮查看体验。
      - **父母本卡片声音值展示与编辑 (2026-07-08 新增)**: 父母本属性区由原本的 2 列升级为 3 列网格，追加了 `声音值` 🎤 输入框，支持玩家手动随时修改声音偏差值。
      - **多重状态呼吸 Badge (2026-07-08 新增)**: 左下角状态徽章支持体型与声音的多重高亮显示，当声音为临界偏差值时（`[-95, -90]` 或 `[90, 95]`），会自动渲染带呼吸闪烁动画的临近 Badge 提示。
      - **精灵蛋管理分页显示 (2026-06-10)**: 限制了精灵蛋管理列表单页仅展示 10 张蛋卡片（通过 `eggCurrentPage` 及 slice 截取），并在过滤条件改变或删除蛋数据时自动将当前页码归拢或重置。下方渲染了精致的响应式分页控制器（含首页、上一页、页码点击、下一页、末页以及当前数据区间统计）。
      - **全局卡片拖拽与排序 (2026-06-10)**: 
        - 全局卡片（蛋窝卡片、父母本仓库卡片、精灵蛋管理卡片）均支持自由拖动排序。
        - 精灵蛋、父本、母本三个区域分别用独立的 `DndContext` 与 `SortableContext` 包裹，编写了专用的 `onDragEnd` 回调处理器（例如 `handleEggDragEnd`、`handleFatherDragEnd`、`handleMotherDragEnd`），利用 `arrayMove` 对底层数组元素进行重新排序，并由 Electron 数据保存机制无缝持久化至本地物理 JSON 文件。
        - 移除了冗余的导入语句，修正了标识符重复声明引起的 Vite 编译报错。
      - **独立的双列隔离过滤与 Autocomplete 下拉模糊检索 (2026-06-10)**: 废弃了原本共享的父母本过滤，为父本和母本各自声明了 4 个共 8 个独立的过滤字段（包括精灵名、性格、蛋组、体型牌）。将过滤栏的精灵名输入框和性格输入框均替换为了 `Autocomplete` 下拉模糊搜索组件，原生支持拼音首字母/字母拼写和模糊检索，选中后实时过滤表格卡片列表，并可通过各自的“重置”按钮一键清空全部检索状态。
      - **重置与初始化隔离清空 (2026-06-10)**: 彻底隔离了三个 Tab（蛋窝中心、父母本仓储、换蛋中心）下的初始化列表/重置行为。特别是在父母本仓储 Tab 下点击“初始化列表”或“重置”时，仅会重置父母本仓储相关数据，并彻底清空上述 8 个新声明 of 过滤字段，杜绝重置后因检索条件残留而导致列表呈空白状态的问题。
      - **父母本卡片地区形态下拉框遮挡修复 (2026-06-10)**: 修复了 hover 父母本卡片时，“选择配组”遮罩层 (`z-30`) 覆盖整个头像导致左下角“地区形态切换下拉框” (`z-10`) 被遮挡无法点击的交互问题。通过将形态下拉框的外层容器 `z-index` 提升为 `z-40` 并添加 `onClick={(e) => e.stopPropagation()}` 阻止事件冒泡，使得地区形态可以被正常切换，且点击下拉框不会触发不必要的卡片配组状态勾选。
      - **智能繁育配对机制**: 自动在已勾选的父本与母本之间寻找匹配组合。规则为：
        1. 蛋组交集非空（即双方至少有一个相同的蛋组）。
        2. 牌子相同或满足体型包容性配对关系（大粗+单粗嗓门、小粗+单粗嗓门、大婉+单婉转声、小婉+单婉转声、单大块头+普通、单小不点+普通）。对立的体型（如大粗+小粗、大婉+小婉）自动拦截，不予配对。
        3. **临界体型与特化规则 (2026-06-30 升级与拼写修正)**：
           - **概率大粗 / 概率大婉 / 概率小粗 / 概率小婉**：父母都是接近大块头/小不点临界值的粗嗓门或婉转声时（或与对应的单粗嗓门/单婉转声配对且临界），智能配对判定子代为对应的概率牌子，显示醒目的虚线框样式。此处的婉转体型统一修正了旧版误拼为“碗”的错别字。
           - **概率大块头 / 概率小不点**：父母一方是接近临界值的普通精灵，另一方是单大块头/单小不点；或者父母双方都是接近临界值的普通精灵时，智能配对显示为“概率大块头”或“概率小不点”。
           - **一键导入蛋窝转换**：配对的“概率大块头”和“概率小不点”卡片在一键导入蛋窝中心时，其牌子（`brand`）会自动安全地转换为合法的基础牌子“普通”。
        若满足上述条件，自动在面板下方实时渲染出子代精灵蛋配对卡片。子代的系别与形态由母本决定（多形态精灵如 `冬羽雀_春天的样子` 在生成子代蛋时，会通过 `petHelper` 自动还原为基础名 `冬羽雀`）。
      - **双亲同性格筛选与母本卡片分组合并 (2026-07-08 新增)**：
        - **双亲同性格与配对综合筛选**：将“双亲同性格”前置性格匹配功能并入了配对类别下拉选择框中（选项包含“全部配对”、“双亲同性格”、“仅3V配对”、“仅非3V配对”）。
        - **性格专属筛选下拉框**：在智能配对中心新增了专门的“性格筛选”下拉菜单，支持一键过滤出父本或母本包含特定性格的优秀繁育组合。
        - **进化链分组合并**：对筛选出的配对列表依产生的精灵蛋品种（`eggSprite`）进行分组合并。同一进化链同样子的母本（例如波波拉与朵拉拉）繁育相同精灵蛋时将合并在同张卡片中，避免了品种重复冗余。
        - **多双亲左右 Chevron 切换**：合并后的卡片提供 Chevron 左右切换按钮及指示角标，用户可在当前精灵蛋下的多个 (father, mother) 双亲配对组合间进行一键切换，卡片两端父母头像、名字、三围、性格、存放位置以及一键导入数据均联动刷新。不同形态（样子的后缀不同）产生的蛋不同，自动隔离在两个独立大卡片下渲染，杜绝了形态混淆。
        - **一键导入筛选后配对**：顶部的一键导入按钮已改造为深度联动 `filteredPairings`。点击时只将当前页面经过筛选框过滤并勾选的配对组合导入到蛋窝中，不再强行导入其他未筛选出的隐藏配对。
      - **蛋窝中心筛选与交互细节打磨 (2026-07-08 新增)**：
        - **父母同性格筛选**：蛋窝中心筛选栏也新增了“父母同性格”复选框，可快速筛选出父母双方第一性格完全一致的蛋窝点。
        - **现蛋数量智能零点初始化**：在 `handleUpdateStatus` 状态处理器中添加了状态切换拦截。当蛋窝卡片的状态从“非有现蛋”切换为“有现蛋”时，默认现蛋数量 `eggCount` 自动初始化为 `"0"`，且该重置在再次点击相同状态时不会被重复触发，既贴合录入习惯又防止误清空已有计数。
        - **长图导出全量数据非分页还原**：一键导出长图（`handleExportLongImage`）在导出前，React 会感知 `isExporting` 状态，临时解除蛋窝、父母本仓储、精灵蛋的分页 slice 切片限制，将全量筛选列表完整重绘至 Canvas 中，生成后自动恢复为原分页排版。
      - **单配对卡片独立导入与全部导入**: 配对面板上不仅支持导入所选配对，而且在每个配对卡片右下角都配有独立的“导入”按钮。点击此按钮将仅将这一对父母的繁育结果（包括父母的性格、三围、牌子、蛋组等详细映射关系）以“有现蛋”的状态和“无极限蛋”的状态导入至蛋窝中心。
      - **卡片勾选控制与大导入按钮优化 (2026-06-09)**:
        - 调大了每个卡片内的“导入”按钮（`px-3.5 py-1.5 text-[11px]`），并阻止事件冒泡（`e.stopPropagation()`），防止与卡片自身的点击选中动作产生冲突。
        - 支持直接点击繁育卡片任意位置进行“选中”状态切换。
        - 选中时，卡片边框高亮（绿边 `border-emerald-300 ring-emerald-300`），左上角绝对定位显示绿色带勾 `Check` 圆圈。
        - 未选中时，卡片半透明度降低（`opacity-60`），左上角显示灰色空心圆圈，以便进行强烈的视觉对比。
        - 顶部的“一键导入”按钮与选中状态联动，更新为“一键导入所选配对 (X 组)”，仅将已选中的繁育组合导入到蛋窝。
      - **3V 蛋自动识别与排除**: 只有在父母双方设置了完全一致且有效的非“无”三围时，导入蛋窝后的子代蛋才会被自动判定为 `"3V"`；若任一方三围设置为“无”，则自动判定为非 3V 蛋，解决了误判和空值统计漏洞。
      - **蛋窝一键产蛋与删除双向联动 (2026-06-13)**: 在蛋窝卡片上点击“产蛋”按钮，可自动计算精灵最低进化形态，并将父母性格、三围、牌子以及来源蛋窝卡片 ID (`fromNestId`) 映射后一键登记为新的精灵蛋卡片。该动作会使来源蛋窝卡片的现蛋数量加 1，且状态自动切换为“有现蛋”。反之，若在“蛋管理中心”删除由该途径产出的精灵蛋，系统会自动追踪 `fromNestId` 将对应蛋窝卡片的现蛋数量减 1；若数量递减到 0，则自动将该蛋窝状态变更为 `"正在孵，可预约"`（正在孵）。
      - **手动登记三围默认值 (2026-06-13)**: 手动点击“登记精灵蛋”时，父母的三围默认选择由原来的 `["无", "无", "无"]` 变更为 3V 的 `["生命", "物攻", "速度"]`，大大缩减了常规用户的选择时间。
      - **常用性格与蛋组统计表格与智能配组建议 (2026-06-16)**:
        - 新增 `WarehouseStatsTable.tsx` 组件，在父母本管理 Tab 的头部与左右分栏之间渲染，用于统计 14 个蛋组与 8 种常用性格的父母本收集只数（公蓝色字，母红色字）。
        - **大粗与大婉限定统计与切换**：表格仅统计牌子为“大粗”或“大婉”的父母本。表格上方提供了精美的“大粗 🧱 / 大婉 🎵”切换按钮。切换时：
          - 仅对应牌子的数据会被累计入表格和收集进度面板。
          - “可产蛋区域（呼吸灯）”和“配组智能建议”强匹配对应的牌子（大粗配大粗，大婉配大婉），确保黄金配对 100% 繁育对应品牌。
          - 联动下方列表过滤：点击格子不仅过滤性格和蛋组，还会将下方筛选栏的牌子联动设置为当前模式（大粗/大婉），实现数据状态物理闭环。
        - 双蛋组在统计中会同时在两处进行计数以体现实际杂交联通能力，支持暗色模式自适应（蓝底为只有公，粉底为只有母，紫底为公母均有，暂无记录为常规色）。
        - 引入了**可产蛋区域（呼吸灯）智能预测**：若某格子（蛋组 G，性格 N）缺少公或母，但玩家拥有对应蛋组 G 的母本以及拥有性格 N 且能与之有蛋组交集配对的父本时，该格子会带黄色虚线框且带有动画呼吸效果。
        - 引入了**配组智能建议模块**：提取当前缺少种公的配组黄金繁育方（即利用性格 N 的父本与蛋组 G 的母本直接繁育）。
        - 支持**一键选中推荐配对**：点击建议卡片的一键选中，系统会清空父母本仓储栏所有筛选，并自动在仓库中仅勾选这一对父母，同时平滑滚动至下方的“智能繁育配对与一键导入中心”进行一键导入。
        - **父母本仓储独立模式切换与卡片排版美化 (2026-07-08 新增)**：
          - 废弃了全局统一的 `parentsViewMode` 切换按钮。
          - 分别在父本和母本的黑色 Header 标题栏最右侧（各自的“全选”按钮左侧）放置了独立的“卡片/表格”切换按钮（支持各自独立切换，状态持久化）。采用与深色 Header 融为一体的 `bg-slate-800 border-slate-700` 高级微胶囊按钮外观。
          - 在父本和母本仓库的表格模式下，行首精灵头像的最左侧新增了快速勾选复选框（Checkbox），允许在表格模式下直接勾选/取消勾选该精灵参与配对，且做了事件防穿透 (`stopPropagation`)，双击行依然能跳转卡片。
          - **ParentCard.tsx 卡片布局重构美化**：
            - 去除头像右下角多余的绝对定位属性圆圈 Badge，解决由于属性名与系别图标相同而导致卡片上存在两颗相同爱心/萌系徽标的视觉 Bug。
            - 重新设计高度“天平”：将“标准物理区间数据面板”以及“体型/声音达标呼吸状态灯 Badge”移到左下侧，平铺渲染在头像与精灵名下方，“📍 游戏内具体存放位置”紧随其下，在充实左侧展示区域的同时，彻底清除了左侧的空白。
            - 将右侧的“牌子”、“身高”、“体重”、“声音值”四大项变更为完全的垂直单列纵向排列；同时升级 input CSS 类为 `flex-1 min-w-0 w-0` 并为两端图标单位设定 `shrink-0`，结合精简后的 placeholder 文字，完美消除了输入框在窄宽度下遮挡重叠单位的 Bug，使数据一目了然。
            - 将“精灵三围 (stats selector)”和“所属组别 (groups)”移回右半侧渲染在性格选择器下方，使卡片高度与左侧完美对称、比例协调。
        - **统计表格维度对调与建议模块移除 (2026-07-08 新增)**：
          - 彻底移除了“智能配组建议列表”和“直接繁育缺口”统计卡片，并将统计大表格宽度 100% 宽度拉满展示，获得开阔视野。
          - 将统计表格的行 and 列的维度进行对调（转置），更新为：“行 = 性格，列 = 蛋组”，表头左上角首列为“性格 \ 蛋组”，数据渲染也相应做了转置适配。
          - 表头各蛋组列以及性格行支持点击筛选联动底下的仓储库，并且支持累加多选和再次点击反选清空。
          - 增加了十字交叉光轴高亮（Crosshair Highlight）系统：选中性格和/或蛋组时，表格内以十字交叉光轴的形式将相关的行和列高亮，并在交叉格加粗呈现。

    - **多账号数据隔离与备份管理 (2026-06-09)**:
      - **多账号模型**: 新增 `Account`（包含 id, uid, nickname）及 `AccountData`（包含 pets, trades, parents）数据模型，通过 `accountDataMap` 将各个分区的精灵数据、换蛋需求和父母本卡片完全物理隔离。
      - **老数据向下兼容**: 在数据加载（`loadLocalData` / `setupBrowserFallback`）时，对无 `accounts` 标识的旧单账号备份或 localStorage 数据，自动智能包装并迁移至 `default` (默认账号) 分区。
      - **安全防抖保存与切换**: 切换账号时，系统先同步当前活动的 React 状态至全局 Map，再加载目标账号数据，最后更新激活账号 ID。结合 600ms 的防抖 useEffect 自动保存，确保读写数据流平滑不冲突，杜绝跨账号数据污染。
      - **配对中心拼音模糊搜索与 Autocomplete 支持 (2026-06-12)**:
        - 将智能繁育与配对中心的“搜索精灵名”输入框替换为 `Autocomplete` 组件，传入了 `ALL_PET_NAMES` 选项。
        - 优化了列表筛选逻辑（`filteredPairings`），在搜索精灵名时不仅匹配汉字，而且支持拼音首字母和缩写拼写进行实时模糊过滤。
      - **小屏手机端标签与过滤器防换行及横滑优化 (2026-06-12)**:
        - 针对宽度有限的小屏手机端，对主导航 Tab 栏、配对中心筛选栏、父本管理筛选栏、母本管理筛选栏以及蛋管理中心筛选栏的排版进行了优化。
        - 阻止文字换行（`whitespace-nowrap`），并使用 `overflow-x-auto` 和 `.no-scrollbar`，使得各区域控件在超出屏幕时支持横向滚动浏览，且不显示滚动条，避免布局折叠与挤压。

      - **毛玻璃微交互切换 UI**: 右上角导航区加入了支持 Hover 显示及旋转 90 度动画的齿轮图标按钮、磨砂玻璃下拉账号切换卡片，以 Indigo 背景和打勾 SVG 标识当前激活账号。
      - **智能导入解析**: 支持单账号和全量多账号导入。若导入单个账号备份，自动提示用户选择“覆盖当前账号”或“作为新账号导入”（可自定义新 UID 和昵称）。
      - **亮/暗色模式切换与全站适配 (2026-06-13)**:
        - **状态管理与偏好持久化**: 引入亮暗色一键切换，采用 `theme` 状态并利用 `useEffect` 同步 `.dark` 类至根节点 `document.documentElement`，并自动把用户选择持久化在 `localStorage`，同时支持跟随系统模式。
        - **全量 UI 适配**: 针对 `Autocomplete`、`SortableCard`、`EggCard`、`ParentCard`、`SortableRow` 以及主页面 `App.tsx` 内 6 个核心模态框（Reset, Account, ImportConfirm, Import, Export, Image-Preview, About）、分页器组件的背景色、边框、文字、高亮及输入元素进行了精细的 `dark:` 变体样式补全。
          - **全站暗色盲区细节优化与对比度拉满 (2026-06-16)**: 彻底消除了最外层页面背景、主容器外壳、自建换蛋需求中心标题与表单大卡片、以及蛋窝点和父母本底部的两组批量操作工具栏在暗色模式下的亮白遗漏；修复了自建需求中心性格需求 Label 与 Autocomplete 输入框 `dark:` 样式的缺失，以及辅助文字提示标签（属性、蛋组）在暗色下大白底的突出缺陷；做到了深色模式全覆盖，保障了极佳的暗室文字可读性。
          - **Tailwind 非标色值 typo 批量修复 (2026-06-16)**: 针对项目中广泛存在的 `*-955/` 等 22 处无效 Tailwind 颜色类进行了批量正则重构和纠正（修复为 `*-950/`），使原本在暗色模式下失效的淡背景、阴影以及高亮色调恢复正常。
        - **长图导出完全支持暗色模式**: 长图导出逻辑中彻底移除了临时强制剥离 `dark` 类的降级逻辑，使导出的图片完全支持暗色模式渲染；同时在 Canvas 绘图渲染中，通过动态计算 `backgroundColor`（暗色下为 `#020617`，亮色下为 `#f8fafc`），确保了长图生成时底板与卡片具有完美的明暗层次与可读性。
        - **三围属性与头像白块消除**: 将各卡片组件中三围圆形指示图标样式在暗色下由原亮色底（如 `bg-rose-200`）自适应适配为更柔和的带半透明深色底（如生命的 `dark:bg-rose-950/40` 等），完美消除暗室大白圈；同时修复了换蛋交易中心表单头像、系别容器的白块遗漏，并将应用默认主题配置修改为 `'dark'`（默认开启暗色模式）。

      - **全局卡片复制粘贴 (Ctrl+C/V) 与智能映射机制 (2026-07-02 新增)**:
        - **基本逻辑**：用户选中旧卡片按 `Ctrl+C` 复制数据，对着另一个卡片按 `Ctrl+V` 可直接粘贴覆盖该卡片。三类卡片（蛋窝卡片、父母本卡片、精灵蛋卡片）全站通用。
        - **空白点击清除与智能高亮**：被点击的卡片会应用靛蓝色光晕边框高亮（`ring-2 ring-indigo-500`），点击页面空白处时触发全局捕获自动清除当前选中。
        - **焦点保护机制**：在捕获全局 `keydown` 快捷键时，若当前焦点元素为 `input`, `textarea`, `select` 等文本编辑框，系统自动释放并保留原生的文字复制粘贴功能，防止被卡片级别拦截。
        - **无目标智能粘贴**：当用户在没有 Hover 或点击选定任何卡片的情况下按 `Ctrl+V` 时，系统会判断当前活动的 Tab，在当前 Tab 列表末尾自动创建相应类型的新卡片并填充映射的数据。
        - **跨卡片跨类型字段智能映射**：
          - **蛋窝 -> 父母本**：若粘贴至父本，提取蛋窝父方属性；若粘贴至母本，提取母方属性；直接在 parents 空白处粘贴新建，则默认创建母本（因为蛋窝名通常是母本名）。
          - **父母本 -> 蛋窝**：若源是父本，覆盖目标蛋窝父方字段；若源是母本，覆盖目标蛋窝母方和精灵主体名字段。
          - **精灵蛋 -> 父母本**：若粘贴至父本，复制 `fatherNature` 和 `fatherStats` 到 `nature` 与 `stats`；若粘贴至母本，复制蛋名 `sprite`、`motherNature` 和 `motherStats`；若 parents 处直接粘贴新建，则默认以母本新建。
          - **精灵蛋与蛋窝的转换**：蛋窝转精灵蛋时，精灵名字自动转换成进化链最低阶形态（使用 `getLowestStageName`）。

      - **数据源统一与自动纠偏机制 (2026-07-06 升级与系别合并)**:
        - **废弃旧数据源**：彻底去除了对旧的 `洛克王国_蛋组精灵表.json`、`全图鉴.json` 以及 `PET_EGG_CONF.json` 数据源文件的引用。
        - **新数据源整合与自包含系别**：改为使用 `src/pets_data.json` 统一管理精灵形态、进化链、系别以及蛋的基础极值。已把系别数据（原 `src/pet_types.json` 内的映射）通过合并逻辑物理写入了 `src/pets_data.json` 的顶层属性与各 `forms` 子属性中，使数据源完全自包含，并收纳至 `src/` 目录。
        - **废弃并删除 `src/pet_types.json`**：彻底物理删除了 `src/pet_types.json` 这一临时映射文件。重构 `src/petHelper.ts`，使其在读取各形态属性时直接提取自包含的 `form.types || []`，保持系别属性 Badge 图标展示与过滤功能的完全动态化。
        - **浮点物理化计算**：舍弃了以往对身高体重 `~` 解析与除以 100/1000 换算的杂乱逻辑，直接读取新 JSON 的浮点数物理大小和极值（giant/tiny weight line）。
        - **内存级数据纠偏与清洗**：在 `petHelper.ts` 预处理阶段，系统自动深拷贝并修正了月亮砣 (ID 238) 的错位数据（过滤脏蛋组，并将错误放大了一万倍的 `weight_max` 16000 纠正为 `1.6kg`，`giant_weight_line` 15869 纠正为 `1.5869kg`），使得前台及格计算和极值渲染完全对齐。
        - **图片资源防污染**：带 `id-` 前缀的图片由于是数据源图，不进入 `images/sprites/` 头像库，头像依然使用干净 of 无前缀名称进行物理名和前缀匹配（映射在 `sprite_files.json`）。

      - **高级数据集成查询与行商模块 (2026-07-09 新增)**:
        - **精灵数据图鉴**：模糊匹配拼音/缩写/汉字，展示大头像、属性系别、蛋组，并用彩色进度条直观展示六项资质资质（生命、速度、物攻、物防、魔攻、魔防）及其总和。进化链支持点击交互，可直接一键跳转搜索节点。
        - **蛋组精灵筛选**：支持单/双蛋组交叉检索，瀑布流展示符合条件的精灵卡片及其系别与标准体积参数。
        - **孵蛋体积预测**：输入精灵蛋的直径与重量（支持非结构化带有单位的文本，如 `120cm 4500g 同乘`），自动运行马氏距离多元高斯似然估计算法，计算其最可能孵化出的精灵并依概率降序排列。
        - **行商悬浮 Widget**：页面右下角常驻可折叠/隐藏的磨砂玻璃悬浮窗，拥有呼吸灯指示及手动/自动（3分钟间隔）刷新功能。通过调用 Electron IPC `window.electronAPI.httpGet` 抓取远行商人的 HTML 文本，前端正则提取出时段商品数据（包含名称、价格、限购、原图和描述）。

2. **主进程 (Electron - CommonJS)**:
   - 文件入口设在 `electron/main.cjs`。由于根目录配置了 `"type": "module"`，Electron 的 CommonJS 代码必须显式命名为 `.cjs` 以确保兼容性。
   - 通过 `preload.cjs` 暴露出安全的 IPC 接口：`loadData()`、`saveData(data)`、`getDataPath()`。
   - **智能存储逻辑**:
      - **存储位置**: 启动与运行中，仅允许在自定义路径下或 EXE 同级目录下保存 `roco_egg_data.json`。废弃了原本静默降级写入 AppData 目录的逻辑。
      - **异常报错弹窗**: 当物理保存失败（如目标目录无写入权限、被杀毒软件拦截）或修改目录文件写入失败时，主进程会拦截异常并强制弹出系统级错误提示窗 (`dialog.showErrorBox`) 告知用户失败原因与排查建议。
      - **历史数据自动迁移 (2026-07-08 新增)**: 启动加载时，若当前默认保存路径（EXE 同级）不存在数据文件，但用户系统 AppData 路径下存有旧版数据备份，程序会尝试自动将其复制迁移至 EXE 同级目录下。如拷贝失败（无写权限），则安全加载 AppData 旧数据防止白屏，但在后续保存时会因写入 EXE 目录失败触发报错弹窗提示。
      - **依赖环境自动探测与引导安装 (2026-07-09 新增)**:
        - **检测机制**：启动时在拉起窗口前依次探测 Python 3 环境（执行 `python --version`）与 Npcap 驱动（通过服务查询 `sc query npcap` 及 `wpcap.dll` 文件检查联合判定）。
        - **离线安装定位**：若缺失环境，自动从便携式 EXE 同级目录、打包根目录或工作目录中定位 `python-3.12.10-amd64.exe` 和 `npcap-1.88.exe` 安装包。
        - **安装交互**：支持 Python 一键静默安装（后台运行带有 `/quiet InstallAllUsers=1 PrependPath=1` 参数）和 Npcap 交互引导安装。若安装成功，主进程会动态检索新生成的安装路径并将其刷新至当前 Node.js 的 `process.env.PATH` 中，实现环境无缝加载；若用户在警告后选择不安装，则允许其继续运行网页/粘贴模式，以提升容错性。
      - **关闭托盘化行为重构 (2026-07-09 新增)**：
        - **异步拦截获取复选框**：将主窗口 close 事件中同步的 `dialog.showMessageBoxSync` 修改为异步的 `dialog.showMessageBox`，正确提取 resolve 对象的 `{ response: choice, checkboxChecked: remember }`；修复了原先无论用户是否勾选均因为 `const remember = (choice === 0 || choice === 1)` 误判为 true 导致强制写入配置直接退出托盘的重大 Bug。
        - **重置关闭偏好 IPC**：增加了 `reset-close-choice` 的 IPC 监听器，接收前端发来的指令将内存中及 `app_config.json` 中的 `rememberCloseChoice` 擦除并重置为 `null`。

3. **打包配置 (`package.json`)**:
   - 使用 `electron-builder` 打包。
   - 打包指令：`npm run electron:build`
   - 生成目标：单文件便携绿色版 (`portable`)，生成于 `dist-electron/` 文件夹下。
   - **打包资源包含与体积瘦身**: 
     - **排除冗余文件**：在 `build.files` 中排除了 `dist/images/sprites_backup/**`，并且修正了直接引入外部 `images/**/*` 的双重打包逻辑。所有外部依赖模块被正确归类至 `devDependencies`（因为运行时渲染进程和 Electron 主进程不需要它们），成功避免了 `node_modules` 被打包进 EXE。这两项优化使 EXE 体积从 **498MB** 大幅缩减至约 **288MB**。
     - **解包外置与路径解析修正**：将 `dist/images/**/*` 通过 `build.asarUnpack` 配置排除在 ASAR 主包外，释放至 `app.asar.unpacked` 中。配合此结构，渲染层 `src/petHelper.ts` 中的 `getImagePath` 从原本针对本地 file 协议特判的 `../images/...` 逻辑统一重构为相对当前路径 `./images/...`，确保网页端部署、本地开发与打包后的 EXE 单文件在读取图片资源时的相对路径完全一致，彻底解决了图片裂开的问题。

---

## 📂 核心文件目录

- `electron/main.cjs` - Electron 主进程逻辑（窗口控制、文件存盘、路径权限探测）
- `electron/preload.cjs` - 预加载脚本，桥接 IPC 数据
- `src/App.tsx` - 主应用界面与数据加载/自动保存状态逻辑
- `src/types.ts` - 补充了 `window.electronAPI` 接口的 TypeScript 声明
- `package.json` - 项目元数据与打包构建脚本配置
- `vite.config.ts` - Vite 的基础路径相对化设置

---

## 💾 数据存盘规则与恢复

1. **自动保存（防抖及关闭保护）**：在渲染层通过 `useRef` + `useCallback` 实现对 `pets` 列表及水印参数等 12 个核心状态变动的实时防抖侦听。用户有任何数据变动将立即触发自动保存，延迟 500ms 执行磁盘与 `localStorage` 物理写入，有效减轻连续输入/拖动时的 IO 开销，杜绝打字卡顿。同时，通过监听 `beforeunload` 事件，在用户刷新、关闭网页或退出 Electron 桌面程序前，会瞬间同步执行存盘，保证最末次变动数据绝不丢失。
2. **状态指示器**：网页右上角的 Credits 区域旁配有实时绿/黄呼吸灯。
   - 绿色：已保存，同时在下方以等宽字体展示本地文件绝对路径（如：`存储路径: D:\desk\洛克王国孵蛋表\data\roco_egg_data.json`）。
   - 黄色：正在写入中。

---

## ⚡ 调试与构建命令

- **前端调试 (网页模式)**:
  `npm run dev`
- **Electron 调试 (桌面窗口模式)**:
  `npm run electron:start`
- **EXE 打包构建**:
  `npm run electron:build`
  - 打包生成的文件存放于：`dist-electron/洛克王国孵蛋数据管理系统 0.0.0.exe`

---

## 📦 代码托管与项目管理

该项目已成功托管至 GitHub 并关联远程仓库进行管理。
- **远程仓库**: `https://github.com/PAIZ1999/roco_egg_master.git`
- **本地/远程分支**: `main` (已成功重命名并与 `origin/main` 建立追踪关系)
- **忽略规则**: 已在 `.gitignore` 中配置过滤 `node_modules/`、`dist/`、`dist-electron/` 以及 `release/`（包含超大编译可执行文件，防止超出 GitHub 100MB 单文件限制）等目录。
- **管理规范**: 详细的上传与 GitHub 看板管理教程已保存在本地根目录的 `github_management_guide.md` 中。
- **Commit 规范**: 建议遵循 Angular 提交规范（如 `feat:`, `fix:`, `docs:`, `refactor:`），并利用 Smart Commits (如 `close #12`) 联动关闭 GitHub Issues。
- **开发与部署经验**: 本地代理运行在 10808 端口上，可在本地 Git 配置中执行 `git config --local http.proxy http://127.0.0.1:10808` 确保顺畅推送与拉取。

---

## 🎨 主题自适应适配记录 (2026-07-09)

### DataQueryTab.tsx 全站亮色/暗色适配
- **头部导航栏**：亮色下改为 `bg-white/80` 白色毛玻璃底板 + `border-slate-200` 边框；子 Tab 切换区背景改为 `bg-slate-100` 浅灰；未激活按钮文字在亮色下显示深灰 `text-slate-500`。
- **搜索输入框**：亮色下改为 `bg-slate-50`、`border-slate-200`、`text-slate-800`，图标颜色跟随切换。
- **精灵图鉴结果区**：
  - 左侧精灵卡片面板：亮色 `bg-white`，头像底座 `bg-slate-50`，精灵名称 `text-slate-800`，ID徽章 `text-indigo-600 bg-indigo-50`，系别/蛋组徽章改为淡蓝色底板。
  - 右侧种族资质面板：亮色 `bg-white`，资质总和数字改为深绿 `text-emerald-600`，进度条底轨 `bg-slate-100 border-slate-200`。
  - 进化链容器：亮色 `bg-white`，按钮亮色下为浅灰底 `bg-slate-50`，悬停 `hover:bg-slate-100`。
  - 精灵/蛋尺寸数值面板：亮色下数值区为 `bg-slate-50`，数值文字深色，达标线标记改为 `amber-50/200`（橙黄） 和 `cyan-50/200`（青蓝）浅色背景 + 深色文字。
- **蛋组筛选 Tab**：精灵卡片改为亮色 `bg-white`，名称深色，图标头像底座 `bg-slate-50`。
- **孵蛋预测 Tab**：结果表格改为 `bg-white`，表头区 `bg-slate-50`，分割线 `divide-slate-100`，行 hover `hover:bg-slate-50`，精灵名称深色。体型标签大块头/小不点/普通均补充亮色变体。

### MerchantFloatWidget.tsx 与独立窗口视觉和交互优化
- **喵喵气泡暗色修复**：将喵喵气泡的 `dark:bg-slate-850` 修改为标准 Tailwind 颜色 `dark:bg-slate-800`，彻底解决暗色下气泡背景失效回退成纯白的问题。
- **防止渲染变形挤压**：移除了独立窗口模式（`isStandalone === true`）下的多重 `p-2` 内边距，改用 `p-0` 结合 `flex items-center justify-center`。同时将 Electron 初始和折叠态的窗口高度由 `75px` 调整为完美的正方形 `70px`，使桌面宠物在折叠时呈现与软件内一致的精美正圆形（不再因可用空间过窄被压缩为椭圆形）。
- **跨窗口主题（亮暗色）同步**：在 `App.tsx` 中向全局注入了对 `storage` 事件的监听器。每当主窗口修改了本地存储的主题设置（`localStorage.getItem('theme')`），桌面独立悬浮窗将立刻接收事件并调用 `setTheme` 刷新状态与 `document.documentElement`，实现亮暗色无缝跟随。
- **聊天窗状态拖拽支持**：在 `MerchantFloatWidget.tsx` 里新增了专为聊天面板标题栏定制的鼠标按下拖动事件处理器 `handleHeaderMouseDown`，并为该 `div` 元素设置 `cursor-move select-none` 样式。现已实现不管是折叠悬浮球状态，还是展开聊天框状态（通过按住顶部标题栏），用户都可以随心所欲地拖拽桌面独立窗口的位置。

### main.cjs 系统托盘与独立窗口重置
- 托盘右键新增“重置关闭确认提示”菜单，一键清除已存储的“记住退出选择”偏好，允许重新弹出关闭选项询问框。
- 调整了创建独立窗口 `createFloatWindow` 时的默认宽高为 `70x70` 像素，以防止在紧凑状态下渲染溢出或被挤压。


### 古钟蛇多形态导入与错误显示智能纠偏 (2026-07-10 新增)
- **多形态导入后缀保留 ([RocoImportModal.tsx](file:///d:/desk/洛克王国孵蛋表-副本/src/components/RocoImportModal.tsx))**：在根据 ID 匹配基础精灵名后，提取 `item` 中可能存在的具体形态后缀（如括号 `（本来的样子）` 或下划线），如果存在且名字暂无后缀，将其规范还原为 `基础名_形态名` 格式（如 `"古钟蛇_本来的样子"`）。这彻底解决了包括古钟蛇、冬羽雀、丢丢、板板壳在内的所有多形态精灵在从 SQLite/API 导入时由于 ID 查名导致形态被抹去的共性问题。
- **图片匹配前缀纠偏 ([petHelper.ts](file:///d:/desk/洛克王国孵蛋表-副本/src/petHelper.ts))**：在 `getSpriteFileName` 中，当前缀模糊匹配 `startsWith(精灵名 + "_")` 命中多个形态图片文件时，**优先寻找并返回**包含 `"本来的样子"` 或 `"平常的样子"` 的图片文件。这保证了即使在手动录入或数据格式中只有基础名 `"古钟蛇"`，前台渲染图片时也能完美智能纠偏至 `"古钟蛇_本来的样子.png"`，消除了以前因 `find()` 返回第一个文件而误显示成 `"古钟蛇_本命年的样子.png"` 的 bug。


### 洛克王国助手 roco_helper 文件名版本兼容 (2026-07-10 新增)
- **动态扫描匹配 ([main.cjs](file:///d:/desk/洛克王国孵蛋表-副本/electron/main.cjs))**：在主进程 `startRocoHelper()` 中废弃了原先对特定文件名 `"roco_helper-v3.2.2.exe"` 的多重路径硬编码，升级为自动扫描路径探测链上的各个有效目录，并利用 `fs.readdirSync` 与 `file.startsWith('roco_helper-')` 进行动态模糊匹配，搜寻首个存在的助手 exe 文件（如更新后的 `roco_helper-v3.2.4.exe` 等）。
- **动态进程生命周期 ([main.cjs](file:///d:/desk/洛克王国孵蛋表-副本/electron/main.cjs))**：在检测到匹配的助手 exe 后，将其动态记录在全局变量 `detectedHelperFileName` 中，在后续的 `tasklist` 进程查重和 `taskkill` 清理中，全部使用此变量动态查杀，保证了即使助手版本升级也能完美静默拉起并在退出时彻底关闭。
- **窗口标题模糊前缀 ([main.cjs](file:///d:/desk/洛克王国孵蛋表-副本/electron/main.cjs))**：Powershell 执行命令中激活秒杀初始弹框的标题匹配从固定的 `'洛克助手 v3.2.2'` 缩短为通用前缀 `'洛克助手'`。由于 AppActivate 匹配标题前缀，这使得任何版本助手的弹窗均能正确被激活和静默关闭。

---

## 📝 多形态与变体样子精灵汇总 (2026-07-11)

为方便开发与查阅，以下是系统中所有拥有不同样子（多形态/亚种变体）的精灵映射汇总：

- **大体量多形态精灵（≥ 4 个形态）**：
  - **鸭吉吉 / 鸭吉吉国王**：蓬松的样子、紧实的样子、急急急鸭、燃了鸭、等一等鸭、起来鸭
  - **晶石蜗 / 钻石蜗**：西瓜碧玺的样子、莲花刚玉的样子、星彩榴石的样子、火山琉璃的样子、蓝锥矿的样子、烧蓝黄金的样子
  - **圣代甜甜**：樱桃草莓口味、樱桃抹茶口味、蓝莓草莓口味、蓝莓巧克力口味、蓝莓抹茶口味、杨桃巧克力口味、杨桃抹茶口味、杨桃草莓口味
  - **蹦蹦种子 / 蹦蹦果 / 蹦蹦花 / 蹦蹦草**：海神球形态、彩玉球形态、短毛球形态、象牙球形态
  - **化蝶**：平常的样子、幽冥眼的样子、喵喵的样子、奇丽花的样子
  - **海枝枝**：碧蓝珊瑚、杏黄百合、翠绿纶布、洋红沙丁

- **环境/三态变体精灵（3 个形态）**：
  - **丢丢 / 卡卡虫 / 卡瓦重**：沙地附近的样子、草地附近的样子、雪山附近的样子
  - **冬羽雀 / 岚鸟 / 雪绒鸟 / 霜翼领主**：春天的样子、夏天的样子、秋天的样子

- **双态/两极状态变体精灵（2 个形态）**：
  - **极昼/极夜**：乌拉塔、乌达、迷你乌（极昼的样子、极夜的样子）
  - **上弦/下弦**：月亮砣、刺轮砣（上弦的样子、下弦的样子）
  - **星光/月光**：小星光、星光狮（星光能量的样子、月光能量的样子）
  - **山地/崖间**：小狮鹫、皇家狮鹫、神圣狮鹫（高山地的样子、崖间地的样子）
  - **枯水/储水**：地鼠、遁地鼠、遁鼠（枯水期的样子、储水时的样子）
  - **蜕皮成长**：板板壳、咔咔壳、水泡壳（蜕皮时的样子）
  - **睁眼/闭眼**：幽冥眼、暗影灵面（睁眼的样子、闭眼的样子）
  - **磨损状态**：刺盔虫、海盔虫、千棘盔、千棘海针（磨损的样子）
  - **枯叶/金黄**：旋叶虫、蓬叶虫、风滚暮虫（枯叶的样子、金黄的样子）
  - **污染变异**：波波螺、消波螺、嗜波螺（被污染的样子）
  - **旧衣/睡衣**：梦悠悠、梦游（穿旧睡衣的样子）
  - **棋子黑白**：棋棋、棋骑士、棋齐垒、棋祈督、棋绮后（白子、黑子）
  - **特殊配饰**：香草甜甜（杨桃饰品、蓝莓饰品）
  - **信使特化**：落陨星兔（信使精灵）
  - **其他形态**：加油蟹（单只海葵的样子）、石肤蜥/石刺蜥/石冠王蜥（球球尾巴的样子）


