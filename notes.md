# Notes: 洛克王国起来鸭与丢丢导入形态解析修复笔记

## 1. 起来鸭与紧实的样子映射对调 Bug 分析 (2026-07-10 新增)
- **原因分析**:
  - 我们在此前任务中，为鸭吉吉建立的 `conf_id` 映射存在对应值对调的问题：
    - 旧代码中 `410738` 被映射为了“紧实的样子”，`300710` 映射为了“起来鸭”。
    - 经查助手接口 `/api/pet-info?uid=1036106` 及其实际网页 DOM 展示，那只在 `1盒 1行5列` 且在助手网页中被明确显示为“起来鸭”形态的 15级/14级鸭吉吉，在 API 和 SQLite 数据中的 `conf_id` 物理上就是 `410738`！
    - 这铁证如山地表明，`410738` 真实对应的形态名称就是 `"起来鸭"`。而原来的 `300710` 对应的才是 `"紧实的样子"` (即戴睡帽的鸭吉吉)。
    - 由于原来的错误映射，导致这只看起来鸭在导入软件后被误识别为 `"鸭吉吉_紧实的样子"`，并在配图时因缺少紧实样子的图而降级为了普通蓬松头像。
- **解决方案**:
  - 对调 `410738` 和 `300710` 的映射值。
  - 新增 `300463` $\rightarrow$ `"等一等鸭"` 的特殊映射以完善鸭吉吉亚种形态匹配。

## 2. 丢丢草地附近的样子导入变成沙地附近的样子 Bug 分析 (历史归档)
- **原因分析**:
  - 导入组件未对丢丢进行 `conf_id` 映射，导致无形态后缀。模糊规则因拼音首字母将无后缀的 `"丢丢"` 错配为排在第一位的 `"丢丢_沙地附近的样子.png"`。
- **解决方案**:
  - 新增丢丢进化链的特殊 `conf_id` 映射：`2200004` -> "草地附近的样子"，`329` 前缀 -> "沙地附近的样子"，`300036` -> "雪山附近的样子"，`410036` -> "火山附近的样子"。
