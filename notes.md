# Notes: 呼吸灯标签位置移动与输入框遮挡优化笔记

## 1. 呼吸灯位置移动
*   **当前逻辑**：`getStatusBadge()` 渲染体型判定和声音达标的 Badge，在极限达标或近及格等状态时有呼吸灯特效。
*   **重定位**：从右侧栏的最上方移出，垂直挂载在左侧一栏 `guideSize` 圆角大面板与 `📍 游戏位置` 面板之间。
*   **排列层级**：
    1. 头像 + 名字 + 属性
    2. 指标数据大面板
    3. `getStatusBadge()` 呼吸灯提示（如“极限大”、“大块头达标”等）
    4. `📍 游戏位置`

## 2. 占位符遮挡与重叠原因剖析
*   **重叠原因**：
    - `input` 标签在 Flex 容器内部声明了 `w-full` 但没有声明 `min-w-0 w-0` 或 `flex-1`。
    - 这造成 input 强占 100% 空间，无视并覆盖了同一 Flex 行内右侧的单位绝对高度或包裹元素（如 `m`、`kg`）。
    - placeholder 文本 `"输入身高..."` 因点点点和字数稍多而显得偏长，在大仓储双栏狭窄视图下容易发生侧边切断。
*   **优化策略**：
    - 将 input 改为 `flex-1 min-w-0 w-0`。
    - 两端组件统一附带 `shrink-0` 杜绝尺寸流失。
    - 精简 placeholder 为 `"输入身高"`、`"输入体重"`、`"输入声音值"`。
    - 通过这两步组合，即使输入框在小屏幕或双分栏极端宽度下，也能将占位符百分之百完美显露，不再被遮盖。
