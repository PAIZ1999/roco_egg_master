# Notes: 洛克王国多形态与分支进化精灵显示错乱 Bug 分析与解决

## 1. 数据查询中心与桌面助理多形态错乱 Bug 分析 (2026-07-11 新增)
- **原因分析**:
  - **索引映射错位**: 在 `DataQueryTab.tsx` 和 `MerchantFloatWidget.tsx` 中，多形态切换按钮（画廊）的渲染逻辑采用的是 `avatars.map((avatar, idx) => { const formItem = forms[idx]; ... })`。这默认假定了 `avatars` 数组与 `forms` 数组在顺序和长度上是严格等价的。
  - 然而，`avatars` 数组是动态扫描 `sprite_files.json`（图片文件名）生成的；而 `forms` 数组则是从 `pets_data.json` 静态加载并动态混入 `pets_race_data.json` 中的区域形态得来的。
  - 由于两者生成方式完全不同，其长度和顺序极不一致。例如对“晶石蜗”来说：
    - `avatars` 数组包含了 6 个具体样子的图片信息（西瓜碧玺、莲花刚玉、星彩榴石、火山琉璃、蓝锥矿、烧蓝黄金）。
    - 动态解析后的 `forms` 数组则包含了 8 项以上（矿晶虫、晶石蜗基础版、多项重复的晶石蜗基础版，以及 6 个具体样子的形态配置）。
    - 按索引 `idx` 映射时，点击后几项样式按钮实际拿到的 `formItem` 却对应到了前面的 `矿晶虫` 或 `晶石蜗（普通版）`，从而造成资质六围、达标及格线展示错乱。
  - **数据源重复错误**: `pets_data.json` 文件中，对于 `39 (矿晶虫)`、`234 (脆筒甜甜)` 等多形态/分支进化精灵，其 `forms` 和 `evolution_chain` 数组中包含了大量的重复数据（例如 6 个同名同属性的 `"晶石蜗"` 副本），这加剧了数组长度不一致的现象。

- **解决方案**:
  - ** deduplicate 物理数据源**: 编写并执行脚本对 `src/pets_data.json` 的 `evolution_chain` 和 `forms` 字段进行原地去重，保持纯净的基础结构。
  - **基于名称进行关联匹配**: 摒弃基于 `idx` 的危险关联，改为通过形态名称及样式进行智能匹配：
    - 若 `avatar.styleName` 为 `"本来的样子"` 或 `"默认"`，优先关联基础形态。
    - 其它情况，通过 `f.name.includes(avatar.styleName)` 寻找 `forms` 列表中名称包含该样式后缀（如 `"西瓜碧玺的样子"` 或 `"起来鸭"`）的正确形態项。

## 2. 起来鸭与紧实的样子映射对调 Bug 分析 (历史归档)
- **原因分析**:
  - `410738` 实际对应的是 “起来鸭”（戴睡帽的鸭吉吉），旧代码误映射为 “紧实的样子”。
  - `300710` 实际对应的是 “紧实的样子”，旧代码误映射为 “起来鸭”。
- **解决方案**:
  - 互换两者在 `RocoImportModal.tsx` 中的映射。
