# Notes: 父母本卡片美化与切换按钮重定位研究笔记

## 1. 切换按钮重定位与深色化设计
*   **目标位置**：父母本标题栏（黑色背景 `bg-slate-900/95`）右侧的“全选”按钮左侧。
*   **深色底样式**：
    *   外部容器：`bg-slate-800 border border-slate-700 h-6.5 mr-1`。
    *   文字和图标：`text-slate-400 hover:text-slate-200`。
    *   父本选中状态：`bg-slate-700 text-sky-400`。
    *   母本选中状态：`bg-slate-700 text-pink-400`。

## 2. ParentCard 结构性美化与高度天平调整
为了根除卡片左侧由于缺少内容而产生的巨大空隙（白片），并且精简右侧表单层级：
*   **左栏（`sm:col-span-4`）排版扩充**：
    *   精灵头像（去除右下角绝对定位的系别 Badge，以防止与精灵名下方的多系别发生重复显示）。
    *   精灵名称（Autocomplete）。
    *   系别圆徽标行。
    *   所属组别（Egg groups）Badge 行。
    *   游戏内具体存放位置定位栏（📍 5盒 2行3列）。
    *   精灵三围（3V/3围选择小球）。
*   **右栏（`sm:col-span-8`）网格化降噪**：
    *   把“牌子”、“身高”、“体重”、“声音值”这 4 个零散字段包装为 `grid grid-cols-2 gap-2` 的 2x2 网格。
    *   这四个输入项分别高度对齐（均为 `h-7`）。
    *   状态指示灯（极限、达标、声音临近）依然在下方自适应呈现。
    *   精灵性格（Nature）选择行，作为右栏的核心，排在最下方。
*   **底部通栏**：
    *   原有标准物理高度区间和及格指示（大及格、小及格）继续保持在最下方通栏展示。
