# Notes: 洛克王国起来鸭与丢丢导入形态解析修复笔记

## 1. 起来鸭导入变成鸭吉吉蓬松的样子 Bug 分析 (2026-07-10 新增)
- **原因分析**:
  - 在 `src/components/RocoImportModal.tsx` 中，`parseSinglePet` 的解析流程存在时序问题：
    1. 优先根据 `base_conf_id` 在 `pets_data.json` 寻找匹配的 `foundPet.name` 并赋给 `rawName`。对于看起来鸭，由于 `base_conf_id` 在游戏里是 `3742` (鸭吉吉)，但在 `pets_data.json` 里鸭吉吉的 ID 是 `11`，故查不到，`rawName` 此时保持为 `""`。
    2. 计算特殊多形态宠物 `conf_id` 映射：`isYajiji` 匹配成功，`confIdVal` 为 `300710`，形态 `formSuffix` 正确被设为 `"起来鸭"`。
    3. 拼接后缀判断：`if (rawName && formSuffix)` 由于 `rawName` 仍然是 `""` 导致不成立，**错过拼接**。
    4. 兜底名字解码：`if (!rawName)` 成立，从 `item.name` 提取出 `"鸭吉吉"` 赋予 `rawName`。
    最终，`rawName` 被解析成无后缀的 `"鸭吉吉"`，在没有形态后缀的情况下默认指向“蓬松的样子”。
- **解决方案**:
  - 将兜底名字解码逻辑提到形态映射和后缀拼接之前，使形态判断和拼接时 `rawName` 已经有了正确的 `"鸭吉吉"` 基础名，从而能顺利拼接成 `"鸭吉吉_起来鸭"`。

## 2. 丢丢草地附近的样子导入变成沙地附近的样子 Bug 分析 (2026-07-10 新增)
- **原因分析**:
  - `src/components/RocoImportModal.tsx` 仅对鸭吉吉建立了特殊多形态 `conf_id` 映射，对丢丢（以及卡卡虫、卡瓦重）的 4 种野外地区形态完全没有处理。
  - 由于没有形态后缀，导入时丢丢被直接解析为 `"丢丢"`，传给 `getSpriteFileName`。
  - 在 `getSpriteFileName` 中，因为不含后缀，它执行了模糊前缀匹配，在 `spriteFiles` 中过滤出以 `"丢丢_"` 开头的所有图片：`['丢丢_沙地附近的样子.png', '丢丢_火山附近的样子.png', '丢丢_草地附近的样子.png', '丢丢_雪山附近的样子.png']`。
  - 排序后的第一个为 `"丢丢_沙地附近的样子.png"`，故无条件返回沙地附近的样子图片，从而使所有丢丢都被错误显示为了沙地附近的样子。
- **解决方案**:
  - 新增丢丢进化链（丢丢、卡卡虫、卡瓦重，其 `base_conf_id` 为 3040/3041/3042 或 3290/3291/3292）的 `conf_id` 形态判定：
    - `conf_id === 2200004` -> "草地附近的样子" (在卡瓦重上也是该皮肤 ID)
    - `Math.floor(conf_id / 10000) === 329` -> "沙地附近的样子"
    - `conf_id === 300036` -> "雪山附近的样子"
    - `conf_id === 410036` -> "火山附近的样子"
  - 让它们正确生成形态后缀，完美配合 `getSpriteFileName` 精确匹配图片。
