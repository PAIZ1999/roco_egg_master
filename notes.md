# Notes: 洛克王国 S3 数据更新与头像归一化匹配技术

## 1. 2026-07-18 新旧精灵数据合并规则设计

由于我们将从 178 个独立的 `database/*.json` 合并为 React 渲染进程唯一的 `src/pets_data.json`，需确保生成的数据格式与 React 项目已有字段完全兼容：

| 旧字段 | 新数据源来源 | 说明 |
| :--- | :--- | :--- |
| `id` | 外层 `id` | 精灵的一阶图鉴编号 |
| `name` | 外层 `name` | 精灵一阶中文名 |
| `filename` | 从 `avatar` 路径中提取 | 如 `1-迪莫.png`，在 React 运行时头像路径为 `./images/sprites/1-迪莫.png` |
| `types` | 外层 `types` | 主精灵的系别列表 |
| `egg_data` | 外层 `egg_data` 过滤 `has_egg` 字段 | 直径、重量、极限线区间数据，不包含 `has_egg` |
| `evolution_chain` | 外层 `evolution_chain` 过滤掉子分支数组 | 过滤后的纯线性字符串数组，例如 `["喵喵", "喵呜", "魔力猫"]` |
| `forms` | 合并 `[主精灵, regional_forms, lord_forms, other_forms]` | 扁平化合并后包含精灵的所有成长阶段与形态资质和物理数据。每一个子项都计算出对应的 `filename` (从形态的 `avatar` 提取) |

通过这种形式物理合并后，`forms` 已经彻底包含所有形态且每一个形态的 `types`、`egg_groups` 及身高体重极值都是精确的，在 React 加载时不再需要去加载 `pets_race_data.json` 来动态构建，这极大地简化了数据加载生命周期。

---

## 2. 精灵头像归一化匹配寻路算法设计

新版头像名字中不仅带有 ID 前缀，而且包含中文括号 `（` 和 `）`。而 React 内部形态调用或传入的名字包含下划线（例如 `雪绒鸟_春天的样子`），或者是无 ID 的纯文本。
为保证寻路 100% 成功，在 `petHelper.ts` 和 `queryHelper.ts` 的寻路函数中设计**双向归一化绝对对比算法**：

### 归一化规则
对于任意字符串（`petName` 或头像文件名）：
1. 剥离数字 ID 前缀与 `-`（如果存在，如 `18-雪绒鸟` ➔ `雪绒鸟`）
2. 剥离后缀名 `.png`（如果存在）
3. 替换中英文括号为下划线，去除括号字符（例如 `雪绒鸟（春天的样子）` ➔ `雪绒鸟_春天的样子`）
4. 转换为小写并去除两端空格

### 匹配逻辑
1. **精确归一化匹配**：遍历 `spriteFiles` 缓存列表，只要某图片文件名归一化后与目标 `petName` 归一化后强等于 `===`，直接返回该图片。
2. **前缀退化模糊匹配**：若精确匹配失败，提取目标 `petName` 剥离下划线后的基准名（`baseName`，例如从 `鸭吉吉_紧实的样子` 提取 `鸭吉吉`），寻找以 `baseName` 开头的图片列表。
3. **特权兜底**：在以 `baseName` 开头的图片中，优先选择包含“蓬松的样子”、“本来的样子”或“平常的样子”的图片进行兜底，若无则返回第一张图片。
