# 🥚 洛克王国孵蛋数据管理系统 (Roco Incubator Table)

> **全属性宠物、蛋组和性格匹配中心，专为洛克王国孵蛋高端玩家打造的桌面数据管理系统。**  
> 支持本地持久化自动保存、可拖拽排序对齐、一键导出防盗长图以及智能 3V 蛋自动统计。

---

## ✨ 核心特性

- **🎮 高效双向匹配筛选**：支持按宠物名、性格、蛋组、牌子、窝点状态以及是否极限等条件进行多维复合检索，快速定位最匹配的父母。
- **🔀 智能拖拽物理排序**：基于 `@dnd-kit` 开发的表格行拖拽系统，玩家可以通过左侧手柄自由调节宠物物理展示顺序，重新排布孵蛋优先级。
- **🧬 智能 3V 蛋检测与计数**：
  - 系统会自动对比父母的“三围”（生命、物攻、速度等）属性配置。
  - **当父母的三围属性完全一致时，系统会自动将该数据行高亮，并标记为「3V蛋」**。
  - 顶部统计面板会实时显示「3V蛋」的总只数，极大提升培养效率。
- **💾 本地免安装自动持久化**：
  - 系统会在后台默默保护您的心血。任何数据的增删改动都会即时自动保存到本地物理文件夹下的 `roco_egg_data.json` 中。
  - 底部存储路径展示清晰，点击 **「修改」** 按钮可随意指定本地存盘文件夹（如果新文件夹下已有数据则会自动读取，无数据则自动同步写入），完美满足备份需求。
- **📸 一键防盗长图导出**：
  - 点击底部的 **「一键导出长图」** 按钮，系统会智能隐藏所有拖拽手柄、操作按钮和筛选栏等干扰元素，直接渲染为纯净的高清表格长图。
  - 支持后台自由配置**水印内容、水印大小与透明度**，在导出的长图背景中会渲染重叠倾斜的防盗水印，发群里、贴吧交流时再也不怕被盗图。
- **💻 精致的桌面便携体验 (Electron)**：
  - 采用 **`001.ico`** 作为 Windows 应用专属图标。
  - 隐藏了顶部冗余的菜单栏和控制按钮，保持纯净沉浸的客户端操作体验。
  - 精准配置了 **`1420x850`** 的初始窗口大小，并针对窄屏提供横向滚动条自适应保障，确保表格列在任何屏幕下都不会被无故裁剪截断。

---

## 🛠 技术栈

| 类别 | 技术 | 说明 |
|------|------|------|
| **语言** | TypeScript / TSX | 前端主要逻辑与 React 组件 |
| **语言** | JavaScript (CommonJS `.cjs`) | Electron 主进程与预加载脚本 |
| **语言** | Python | 爬虫脚本，用于抓取游戏基础数据 |
| **前端框架** | React | UI 组件化框架 |
| **构建工具** | Vite | 开发服务器与生产构建，配置 `base: './'` 支持 `file://` 协议 |
| **样式** | TailwindCSS | 原子化 CSS 样式框架 |
| **桌面封装** | Electron | 将 Web 应用封装为 Windows 桌面 `.exe` |
| **打包工具** | electron-builder | 生成单文件便携绿色版 EXE |
| **拖拽排序** | @dnd-kit | 表格行拖拽排序功能 |
| **长图导出** | html2canvas | 一键将表格渲染为带水印的高清长图 |
| **数据存储** | 本地 JSON 文件 | `roco_egg_data.json` 自动持久化，支持自定义存储路径 |

---

## 🚀 开发者快速启动指南

### 1. 本地开发调试
1. 确保安装了 **Node.js** 环境。
2. 克隆项目或在工作区根目录下执行：
   ```bash
   npm install
   ```
3. 启动 Vite 本地开发服务器：
   ```bash
   npm run dev
   ```
4. 启动 Electron 桌面调试环境：
   ```bash
   npm run electron:start
   ```

### 2. 构建与打包
- **静态资源编译**（生成静态网页）：
  ```bash
  npm run build
  ```
- **Windows 便携版 EXE 打包**（一键生成自带 `001.ico` 图标的绿色便携版程序）：
  ```bash
  npm run electron:build
  ```
  打包成功后，可在根目录下的 `dist-electron/` 目录中找到 **`洛克王国孵蛋数据管理系统 0.0.0.exe`**。

---

## 🎬 孵蛋管理工具教程视频文案 (配音与分镜稿)

> **文案定位**：极具亲和力、干货满满的洛克王国“孵蛋党”新手/进阶保姆级教程。
> **视频时长预估**：2 ~ 3 分钟
> **适合发布平台**：Bilibili、抖音、贴吧视频等

### 📌 视频分镜与台词设计

| 画面/分镜描述 | 旁白/配音台词 (口语化、生动热情) | 视觉焦点与操作提示 |
| :--- | :--- | :--- |
| **【分镜 1：痛点引入】**<br>画面展示以前用 Excel 手记或手写草稿本的凌乱照片，或者洛克王国皇家研究院孵蛋区域。 | “哈喽各位洛克王国的蛋党大佬和新手朋友们！还在用 Excel 慢吞吞地手敲父母性格，或者每次配蛋都对着仓库里一堆三围、蛋组发呆吗？稍微一粗心，辛苦培育的好底子可能就废了！” | 配合悲伤/叹气的背景音乐，突出传统孵蛋记录方式的繁琐。 |
| **【分镜 2：工具亮相】**<br>屏幕切换到最新的【洛克王国孵蛋数据管理系统】客户端运行画面。默认展示喵喵的 3V 高亮条目。 | “今天给大家安利一款专为咱洛克玩家定制的**「孵蛋数据管理系统」**！绿色便携版，双击就能跑，连安装都不用，还能自定义数据保存位置，从此告别孵蛋丢档的烦恼！” | 展示双击运行桌面上 `001.ico` 图标的 EXE，展现纯净清爽的软件主界面。 |
| **【分镜 3：添加与 3V 检测】**<br>鼠标点击「添加新精灵」，现场演示选择母喵喵、公喵喵，当把两者的三围属性（如生命/物攻/速度）调成一致时。 | “配对特别简单：直接添加宠物，选择对应性格、三围、还有蛋组。注意看！**当父母的生命、物攻、速度等三围选项完全一致时，这一行会自动变成粉黄色高亮，并标记为 3V 蛋！** 顶部统计还会实时计数。出 3V 极品就是这么简单直观！” | 演示点击下拉菜单，三围属性从不一致调整为一致时，行自动高亮的动态过程，以及顶部“3V 蛋”计数器从 0 变成 1。 |
| **【分镜 4：拖拽排序与筛选】**<br>演示鼠标按住左侧的 `::` 排序手柄，上下拖拽宠物行；然后在顶部筛选栏切换“大婉”牌子或“有现蛋”窝点。 | “数据多了也不怕！你可以按住左侧的排序手柄，轻松拖拽调换位置，把最近要孵的宠物排到最上面。顶部还有超强大的多维筛选：点一下‘有现蛋’或者‘大婉’，瞬间过滤完，清爽得不行！” | 鼠标流畅拖动行；在筛选栏选择“大婉”和“有现蛋”，展示数据秒级过滤的清爽效果。 |
| **【分镜 5：修改保存路径】**<br>鼠标移动到软件底部的存储路径区，点击右侧的「修改」按钮，弹窗选择一个本地新建的文件夹。 | “平时不管是添加还是修改，软件都会自动保存。如果你想把数据备份到别的文件夹，直接点底部的‘修改’，选择新的目录，老数据会自动同步过去，安全性直接拉满！” | 演示点击“修改”按钮，弹出 Windows 原生文件夹选择器，选择完毕后，底部路径即时更新。 |
| **【分镜 6：防盗长图导出】**<br>点击“水印配置”，调整透明度，然后点击“一键导出长图”，展示弹出的高清长图预览（带倾斜半透明防盗水印）。 | “最后，想把孵蛋成果发给盟友或者发帖交流？点一下‘一键导出长图’！系统会自动隐藏所有杂乱的按钮，只留纯净表格。而且这里还可以自定义防盗水印的字样、大小和透明度，看这效果，既专业又不用担心被别人盗图发帖啦！” | 演示修改水印字样为“专属孵蛋表-禁止盗图”，点击导出长图，放大展示生成的带斜向重影水印的高清长图。 |
| **【分镜 7：尾声与指引】**<br>画面回到主窗口，鼠标在右上角点×关闭，展现打包后的 EXE 文件。 | “怎么样，有了这个孵蛋神器，是不是感觉神宠培养已经成竹在胸了？打包好的便携版 EXE 就在 `dist-electron` 目录下。快去下载体验吧，祝大家都能一发入魂，孵出心仪的极限神宠！我们下期视频再见，拜拜~” | 播放轻松的结束音乐，画面打出“祝大家发发极限，一发入魂！”的字幕。 |

---

## 🤝 参与贡献

如果您在使用过程中发现任何 BUG 或有更好的优化建议（例如支持更多的性格/属性联动计算），欢迎提交 Issue 或 Pull Request。

*祝所有洛克王国的勇士们，孵蛋顺利，神宠不断！*
